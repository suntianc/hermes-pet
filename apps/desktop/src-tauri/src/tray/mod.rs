/// Stub — will be implemented in Plan 02 with:
/// - TrayIconBuilder with full menu (Show/Hide, TTS, Model, Size, Quit)
/// - Menu event handlers
/// - Dynamic model list updates
pub fn build_tray(_app: &tauri::App) -> Result<(), Box<dyn std::error::Error>> {
    // Placeholder — Plan 02 implements the actual tray logic
    Ok(())
}
