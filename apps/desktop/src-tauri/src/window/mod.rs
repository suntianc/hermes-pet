/// Stub — will be implemented in Plan 02 with:
/// - Bottom-right anchoring via tauri-plugin-positioner
/// - Drag/resize support
/// - Window state persistence via tauri-plugin-window-state
pub fn setup_window(_app: &tauri::App) -> Result<(), Box<dyn std::error::Error>> {
    // Placeholder — Plan 02 implements the actual window management logic
    Ok(())
}
