---
phase: 04-cleanup
plan: 01
type: execute
wave: 1
autonomous: true
requirements: [L2D-09, L2D-10, L2D-11]
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src/features/pet/RiveRenderer.ts (delete)
  - apps/desktop/src/features/pet/rive-inputs.ts (delete)
  - apps/desktop/src/features/pet/model-registry.ts
  - apps/desktop/src/components/PetStage.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/main.tsx
  - apps/desktop/vite.config.mts
  - CLAUDE.md (root)
  - apps/desktop/public/assets/models/models.json
---

<objective>
完全移除 Rive 依赖和相关代码，更新文档反映 Live2D 架构。
All Rive references removed, project clean, documentation updated, build passes.

Purpose: 项目已完成 Live2D 迁移，需要清理所有 Rive 残留代码和依赖。
在此计划完成后，项目不包含 Rive 依赖、代码引用或文档描述。

Output: 清理后的 package.json, 删除的 Rive 文件, 更新的文档, 通过的构建
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Phase 3 已完成 (model-registry.ts 已更新支持 Live2D)。

剩余 Rive 引用:
1. package.json: "@rive-app/canvas": "^2.37.5"
2. src/features/pet/RiveRenderer.ts — 完整文件（未被引用但仍存在）
3. src/features/pet/rive-inputs.ts — 完整文件（未被引用但仍存在）
4. src/features/pet/model-registry.ts — ModelType = 'rive' (Phase 3 已改为 'rive' | 'live2d')
5. src/components/PetStage.tsx — CSS class "rive-container" (line 411)
6. src/App.tsx — ".rive-container" selector (line 425)
7. src/main.tsx — console.log('[Rive] Using auto WASM (dev mode)') (line 56)
8. CLAUDE.md — 大量 Rive 架构描述
9. assets/models/ — models.json 已更新（Phase 3）

注意: Rust 后端 (commands/models.rs) 仍引用 .riv — 但不在本次修改范围内。
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 删除 RiveRenderer.ts 和 rive-inputs.ts</name>
  <files>
    apps/desktop/src/features/pet/RiveRenderer.ts
    apps/desktop/src/features/pet/rive-inputs.ts
  </files>

  <action>
    删除两个 Rive 渲染文件。这些文件已不被任何代码引用（PetStage.tsx 已使用 Live2DRenderer）。

    git rm apps/desktop/src/features/pet/RiveRenderer.ts
    git rm apps/desktop/src/features/pet/rive-inputs.ts
  </action>

  <verify>
    <automated>
      test ! -f apps/desktop/src/features/pet/RiveRenderer.ts \
        && test ! -f apps/desktop/src/features/pet/rive-inputs.ts
    </automated>
  </verify>

  <done>
    RiveRenderer.ts 和 rive-inputs.ts 已从项目删除
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 从 package.json 移除 @rive-app/canvas 依赖</name>
  <files>
    apps/desktop/package.json
  </files>

  <action>
    从 apps/desktop/package.json 的 dependencies 中删除 "@rive-app/canvas": "^2.37.5" 这一行。
    删除后检查 node_modules 是否仍有此包（可以通过 rm -rf node_modules/.cache 来清理）。
    注意：仅移除 package.json 中的行，不执行 npm install。
  </action>

  <verify>
    <automated>
      ! grep -q "@rive-app/canvas" apps/desktop/package.json
    </automated>
  </verify>

  <done>
    @rive-app/canvas 从 package.json dependencies 中移除
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 清理 Rive CSS 类名和日志引用</name>
  <files>
    apps/desktop/src/components/PetStage.tsx
    apps/desktop/src/App.tsx
    apps/desktop/src/main.tsx
  </files>

  <action>
    清理三处 Rive 残留引用：

    1. PetStage.tsx line 411: className="rive-container" → className="live2d-container"
    2. App.tsx line 425: 选择器 '.rive-container' → '.live2d-container'
    3. main.tsx lines 55-57: 移除以注释/日志形式存在的 Rive WASM 日志行
    
    PetStage.tsx 修改:
    ```
    className="rive-container" → className="live2d-container"
    ```
    
    App.tsx 修改:
    ```
    .closest?.('.rive-container') → .closest?.('.live2d-container')
    ```
    
    main.tsx 修改:
    删除或注释以下行:
    ```
    if (baseUrl.startsWith('http')) {
      console.log('[Rive] Using auto WASM (dev mode)');
    }
    ```
  </action>

  <verify>
    <automated>
      ! grep -q "rive-container" apps/desktop/src/components/PetStage.tsx \
        && ! grep -q "rive-container" apps/desktop/src/App.tsx \
        && ! grep -q "\[Rive\]" apps/desktop/src/main.tsx
    </automated>
  </verify>

  <done>
    PetStage.tsx: "rive-container" → "live2d-container"
    App.tsx: ".rive-container" → ".live2d-container"
    main.tsx: 移除 "[Rive] Using auto WASM" 日志行
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 更新 model-registry.ts 移除 Rive 残留引用</name>
  <files>
    apps/desktop/src/features/pet/model-registry.ts
  </files>

  <action>
    Phase 3 已将 ModelType 设为 'rive' | 'live2d'。ModelType 保留 'rive' 
    以向后兼容（用户可能仍有 .riv 格式的模型配置在 app data 中）。
    
    但是，检查 model-registry.ts 中的注释，将所有提及 ".riv" 的注释更新为
    Live2D 格式描述。移除逻辑中对 Rive 的特殊处理（如果有）。
  </action>

  <verify>
    <automated>
      grep -q "ModelType" apps/desktop/src/features/pet/model-registry.ts
    </automated>
  </verify>

  <done>
    model-registry.ts 中 Rive 相关注释已更新，仅保留向后兼容的 'rive' 类型
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: 更新 CLAUDE.md 反映 Live2D 架构</name>
  <files>
    CLAUDE.md
  </files>

  <action>
    更新 CLAUDE.md 中的多处 Rive 引用为 Live2D 架构描述。需要修改的位置包括：

    1. 项目描述（~line 5）: "使用 Rive 引擎渲染" → "使用 Live2D Cubism 5 WebGL 引擎渲染"
    2. 目录描述（~line 28）: "Tauri 2 desktop app (Rust + React + Rive)" → "(Rust + React + Live2D Cubism 5)"
    3. 研究目录（~line 16）: "Rive integration research" → "Live2D integration research" 
       (这是一个不准确描述，应更新反映当前状态)
    4. 架构概述（~line 78）: "Rust + React + Rive" → "Rust + React + Live2D Cubism 5"
    5. model.rs 描述（~line 92）: ".riv file import" → "模型文件导入（.moc3 / .model3.json）"
    6. Rive Rendering 章节（~lines 107-126）: 用 Live2D 渲染章节替代
    7. 文件表（~lines 224-236）: 更新为 Live2D 文件引用
    8. Event Pipeline（~line 154）: RiveRenderer → Live2DRenderer
    9. 模型系统（~line 172）: .riv files → .moc3 模型
    10. Rive 状态机输入 → Live2D 参数/表情
    
    注意：
    - CLAUDE.md 很长（~250行），使用精确的 oldString/newString 匹配
    - 只更新需要改动的行/段落，尽量保持文件其余部分不变
    - 保持文件结构：Monorepo Structure → Commands → Architecture → 各子系统
    
    需要改动的具体位置（使用 grep 精确定位）:
    1. 第 5 行约: "使用 Rive 引擎渲染" 或类似描述
    2. 第 14 行约: 研究目录描述
    3. 第 28 行: 目录列表中 apps/desktop 的描述
    4. 第 78 行: 架构概述
    5. 第 92 行: model.rs 描述
    6. 第 98 行: Rive WASM 加载
    7. 第 107-126 行: Rive Rendering 整个章节
    8. 第 154 行: Event Pipeline RiveRenderer
    9. 第 172 行: .riv files
    10. 第 224-236 行: 文件表
  </action>

  <verify>
    <automated>
      ! grep -q "@rive-app/canvas" CLAUDE.md \
        && ! grep -q "RiveRenderer" CLAUDE.md \
        && ! grep -q "\.riv" CLAUDE.md
    </automated>
  </verify>

  <done>
    CLAUDE.md 中所有 Rive 引用已删除，替换为 Live2D 架构描述。
    Live2DRenderer、Cubism SDK、.moc3 等相关描述已添加。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: 构建验证 (npx vite build)</name>
  <files>
    apps/desktop/
  </files>

  <action>
    运行 vite build 验证构建通过:
    ```bash
    cd apps/desktop && npx vite build
    ```
    
    如果构建失败，根据错误信息修复然后重试。
    预期结果：构建成功，无 Rive 相关错误。
  </action>

  <verify>
    <automated>
      npx vite build 在 apps/desktop 目录运行成功（exit code 0）
    </automated>
  </verify>

  <done>
    vite build 成功，dist/ 目录包含构建产物，无 Rive 错误
  </done>
</task>

</tasks>

<verification>
- Task 1: RiveRenderer.ts + rive-inputs.ts 已删除
- Task 2: package.json 无 @rive-app/canvas
- Task 3: CSS 类名和日志已清理（rive-container → live2d-container, [Rive] 日志移除）
- Task 4: model-registry.ts 注释已更新
- Task 5: CLAUDE.md 已更新为 Live2D 架构
- Task 6: npx vite build 通过
</verification>

<success_criteria>
1. ✓ @rive-app/canvas 从 package.json 移除
2. ✓ RiveRenderer.ts 和 rive-inputs.ts 文件删除
3. ✓ Rive CSS 类名和日志引用全部清理
4. ✓ CLAUDE.md 更新为 Live2D 架构描述
5. ✓ vite build 编译通过，无 Rive 引用残留
6. ✓ 可以切换到 npm install（reinstall/rm node_modules）后构建
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup/04-01-SUMMARY.md`
</output>
