---
id: "01-01"
phase: 1
name: "Rive Runtime Foundation"
wave: 1
depends_on: []
requirements: [RIVE-01, RIVE-02, RIVE-05, RIVE-07]
autonomous: true
---

# Plan 01-01: Rive Runtime Foundation

## Objective

安装 `@rive-app/canvas` 运行时，创建 `RiveRenderer.ts` 实现 `PetRenderer` 接口，实现 Rive WASM 预加载，基础 `.riv` 文件加载和 State Machine 播放。

## Files Modified

- `apps/desktop/package.json` — 添加 `@rive-app/canvas` 依赖
- `apps/desktop/src/main.tsx` — 添加 Rive WASM 预加载
- `apps/desktop/src/features/pet/RiveRenderer.ts` — **新建** Rive 渲染器实现
- `apps/desktop/src/features/pet/rive-inputs.ts` — **新建** SM 输入名称常量
- `apps/desktop/src/features/pet/model-registry.ts` — 添加 Rive 模型类型支持
- `apps/desktop/tsconfig.json` — 添加类型引用（如需要）

---

## Tasks

### Task 01-01-A: Install @rive-app/canvas

<action>
Install the Rive canvas runtime:

```bash
cd apps/desktop
npm install @rive-app/canvas
```

Verify the package is listed in `apps/desktop/package.json` under `dependencies`.
</action>

<read_first>
- `apps/desktop/package.json` — 当前依赖列表
</read_first>

<acceptance_criteria>
- `apps/desktop/package.json` 的 `dependencies` 中包含 `"@rive-app/canvas"`
- `apps/desktop/node_modules/@rive-app/canvas/package.json` 存在
- TypeScript 编译无错误：`import Rive from '@rive-app/canvas'` 可解析
</acceptance_criteria>

---

### Task 01-01-B: Create rive-inputs.ts constants

<action>
在 `src/features/pet/rive-inputs.ts` 创建 State Machine 输入名称常量定义：

```typescript
export const RIVE_INPUTS = {
  STATE: 'state',
  BLINK_TRIGGER: 'blink',
  BREATHE_TRIGGER: 'breathe',
  MOUTH_OPEN: 'mouth_open',
  LOOK_X: 'look_x',
  LOOK_Y: 'look_y',
} as const;

export type RiveStateValue =
  | 'idle'
  | 'thinking'
  | 'speaking'
  | 'happy'
  | 'error'
  | 'searching'
  | 'coding'
  | 'terminal'
  | 'confused'
  | 'angry';

export const RIVE_STATES: { value: RiveStateValue; label: string }[] = [
  { value: 'idle', label: 'Idle' },
  { value: 'thinking', label: 'Thinking' },
  { value: 'speaking', label: 'Speaking' },
  { value: 'happy', label: 'Happy' },
  { value: 'error', label: 'Error' },
  { value: 'searching', label: 'Searching' },
  { value: 'coding', label: 'Coding' },
  { value: 'terminal', label: 'Terminal' },
  { value: 'confused', label: 'Confused' },
  { value: 'angry', label: 'Angry' },
];
```

这些值与 Rive Editor 中 State Machine 的输入命名精确匹配。用户制作 .riv 文件时以这些名称为准。
</action>

<read_first>
- `src/features/pet/` 目录结构
- `src/features/actions/action-schema.ts` — 参考 ActionType 定义
</read_first>

<acceptance_criteria>
- `src/features/pet/rive-inputs.ts` 文件存在
- 文件导出 `RIVE_INPUTS` 常量对象和 `RiveStateValue` 类型
- `RIVE_INPUTS` 包含 `STATE`, `BLINK_TRIGGER`, `BREATHE_TRIGGER`, `MOUTH_OPEN`, `LOOK_X`, `LOOK_Y`
- TypeScript `strict` 模式编译无错误
</acceptance_criteria>

---

### Task 01-01-C: Create RiveRenderer

<action>
创建 `src/features/pet/RiveRenderer.ts`，实现 `PetRenderer` 接口。核心逻辑：

```typescript
import Rive from '@rive-app/canvas';
import { PetRenderer, PlayActionOptions } from './PetRenderer';
import { ModelConfig } from './model-registry';
import { RIVE_INPUTS, RiveStateValue } from './rive-inputs';

interface RiveInstance {
  rive: Rive;
  canvas: HTMLCanvasElement;
  artboard: string;
  stateMachine: string;
}

export class RiveRenderer implements PetRenderer {
  private instances: RiveInstance[] = [];
  private mainCanvas: HTMLCanvasElement | null = null;
  private bgCanvas: HTMLCanvasElement | null = null;
  private currentAction: string = 'idle';
  private disposed = false;

  get view(): HTMLCanvasElement | null {
    return this.mainCanvas;
  }

  async loadModel(modelConfig: ModelConfig): Promise<void> {
    // 清理旧实例
    this.cleanupInstances();

    // 创建角色 canvas
    this.mainCanvas = document.createElement('canvas');
    this.mainCanvas.className = 'rive-character-canvas';
    this.mainCanvas.style.display = 'block';

    // 创建背景 canvas
    this.bgCanvas = document.createElement('canvas');
    this.bgCanvas.className = 'rive-background-canvas';
    this.bgCanvas.style.display = 'block';
    this.bgCanvas.style.position = 'absolute';
    this.bgCanvas.style.top = '0';
    this.bgCanvas.style.left = '0';
    this.bgCanvas.style.pointerEvents = 'none';

    // 加载角色 .riv
    const charBuffer = await this.fetchModel(modelConfig.path);
    const charRive = new Rive({
      buffer: charBuffer,
      canvas: this.mainCanvas,
      autoplay: true,
      stateMachine: 'State Machine 1',
      onLoad: () => {
        charRive.resizeToCanvas();
        this.setRiveInput(RIVE_INPUTS.STATE, 'idle');
      },
    });

    this.instances.push({
      rive: charRive,
      canvas: this.mainCanvas,
      artboard: 'main',
      stateMachine: 'State Machine 1',
    });

    // 加载背景 .riv（可选，从同路径加载 bg artboard）
    try {
      const bgBuffer = await this.fetchModel(modelConfig.path);
      const bgRive = new Rive({
        buffer: bgBuffer,
        canvas: this.bgCanvas,
        autoplay: true,
        artboard: 'background',
        stateMachine: 'Background SM',
        onLoad: () => bgRive.resizeToCanvas(),
      });
      this.instances.push({
        rive: bgRive,
        canvas: this.bgCanvas,
        artboard: 'background',
        stateMachine: 'Background SM',
      });
    } catch {
      // 背景 artboard 是可选的
      console.log('[RiveRenderer] No background artboard found');
    }
  }

  async playAction(actionName: string, options?: PlayActionOptions): Promise<void> {
    this.currentAction = actionName;
    const state = this.actionToState(actionName);
    this.setRiveInput(RIVE_INPUTS.STATE, state);
  }

  async playMotion(group: string, index?: number, options?: PlayActionOptions): Promise<void> {
    this.playAction(group, options);
  }

  async setExpression(name: string): Promise<void> {
    // Rive State Machine 通过 state 管理表情，无需单独设置
  }

  setParameters(params: Record<string, number>): void {
    // Phase 2 实现
  }

  setSpeaking(speaking: boolean, amplitude?: number): void {
    // 空壳 — Phase 2 实现嘴型同步
  }

  async speak(text: string): Promise<void> {
    // Phase 2 实现
  }

  lookAt(x: number, y: number): void {
    // Phase 2 实现鼠标跟随
  }

  resetPointer(): void {
    // Phase 2 实现
  }

  resize(width: number, height: number): void {
    const dpr = window.devicePixelRatio || 1;
    // 角色 canvas
    if (this.mainCanvas) {
      this.mainCanvas.width = width * dpr;
      this.mainCanvas.height = height * dpr;
      this.mainCanvas.style.width = `${width}px`;
      this.mainCanvas.style.height = `${height}px`;
    }
    // 背景 canvas — 全屏覆盖
    if (this.bgCanvas) {
      this.bgCanvas.width = window.innerWidth * dpr;
      this.bgCanvas.height = window.innerHeight * dpr;
      this.bgCanvas.style.width = `${window.innerWidth}px`;
      this.bgCanvas.style.height = `${window.innerHeight}px`;
    }
    for (const inst of this.instances) {
      inst.rive.resizeToCanvas();
    }
  }

  idle(): void {
    this.playAction('idle');
  }

  destroy(): void {
    this.disposed = true;
    this.cleanupInstances();
    this.mainCanvas = null;
    this.bgCanvas = null;
  }

  // ── 内部方法 ──

  private actionToState(action: string): RiveStateValue {
    const validStates: RiveStateValue[] = ['idle', 'thinking', 'speaking', 'happy', 'error',
      'searching', 'coding', 'terminal', 'confused', 'angry'];
    return validStates.includes(action as RiveStateValue) ? action as RiveStateValue : 'idle';
  }

  private setRiveInput(name: string, value: string | number | boolean): void {
    for (const inst of this.instances) {
      try {
        inst.rive.setInputState(inst.stateMachine, name, value);
      } catch {
        // 忽略输入不存在的错误（SM 可能没有这个输入）
      }
    }
  }

  private async fetchModel(path: string): Promise<ArrayBuffer> {
    const response = await fetch(path);
    if (!response.ok) throw new Error(`Failed to load model: HTTP ${response.status}`);
    return response.arrayBuffer();
  }

  private cleanupInstances(): void {
    for (const inst of this.instances) {
      inst.rive.cleanup();
    }
    this.instances = [];
  }
}
```

关键实现细节：
- `loadModel` 创建两 canvas：角色层 + 背景层
- 背景层 canvas 设 `pointerEvents: 'none'` 实现事件穿透
- State Machine 通过 `setInputState` 驱动
- `resize` 处理 `devicePixelRatio`
- Rive 实例创建后立即调用 `resizeToCanvas()`
- WASM 预加载由 Task 01-01-D 保证，此处不处理
</action>

<read_first>
- `src/features/pet/PetRenderer.ts` — 需要实现的接口定义
- `src/features/pet/Live2DRenderer.ts:1-80` — 参考接口方法的签名
- `src/features/pet/model-registry.ts` — ModelConfig 类型
- `.planning/research/FEATURES.md` — Rive API 参考
- `.planning/research/PITFALLS.md` — WASM 加载时序注意事项
</read_first>

<acceptance_criteria>
- `src/features/pet/RiveRenderer.ts` 文件存在
- `RiveRenderer` 类实现 `PetRenderer` 接口（TypeScript 编译通过）
- `loadModel()` 接收 `ModelConfig`，创建 Rive 实例
- `playAction()` 调用 `setInputState('State Machine 1', 'state', value)`
- `resize()` 正确处理 `devicePixelRatio`
- `destroy()` 调用 `rive.cleanup()` 防止内存泄漏
- 多层 canvas：`rive-character-canvas` 和 `rive-background-canvas` 两个 class 的 canvas
</acceptance_criteria>

---

### Task 01-01-D: Rive WASM 预加载

<action>
在 `src/main.tsx` 中添加 Rive WASM 预加载逻辑。Live2D 的 WASM 加载保持不变（Phase 3 才移除）。

```typescript
// 在 bootstrap 函数中追加
import { setWebAssemblyPath } from '@rive-app/canvas';

async function bootstrap(): Promise<void> {
  // 现有 Live2D WASM 加载
  await loadScript('./live2dcubismcore.js');

  // 新增：设置 Rive WASM 路径
  const baseUrl = window.location.href;
  if (baseUrl.startsWith('http')) {
    // 开发模式：@rive-app/canvas 自动管理 WASM
    console.log('[Rive] Using auto WASM');
  } else {
    // 生产模式：设置 WASM 路径到正确位置
    const riveWasmUrl = new URL('../node_modules/@rive-app/canvas/rive.wasm', import.meta.url).href;
    setWebAssemblyPath(riveWasmUrl);
  }

  await import('./styles.css');
  const { default: App } = await import('./App');
  // ...
}
```

注意： `@rive-app/canvas` v2.x 会自动下载 WASM，但显式设置路径确保生产环境可用。如果自动管理正常，可简化。
</action>

<read_first>
- `src/main.tsx` — 当前 WASM 加载入口
- `.planning/research/PITFALLS.md` §WASM Loading Timing
</read_first>

<acceptance_criteria>
- `src/main.tsx` 的 `bootstrap()` 中包含 Rive WASM 初始化逻辑
- 应用启动时无 Rive WASM 加载错误
- 开发模式 `localhost:5173` 下 Rive 实例可正常创建
</acceptance_criteria>

---

## Verification

1. `npm run build:renderer` 编译无错误
2. `npm run dev:renderer` 启动后控制台无 Rive 相关错误
3. 手动创建简单测试：`new Rive({ buffer, canvas })` 在浏览器中运行正常
4. RiveRenderer 实例化后 `view` 属性返回 HTMLCanvasElement

## must_haves

- `@rive-app/canvas` 安装到依赖中
- `RiveRenderer.ts` 可编译，实现了 `PetRenderer` 接口的所有方法
- Rive WASM 在应用启动时成功加载
- 基础 `.riv` 文件可通过 `fetch → ArrayBuffer → new Rive({ buffer })` 加载
- SM 输入 `setInputState('State Machine 1', 'state', 'idle')` 可调用无异常
