---
phase: 01-live2d-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/vite.config.mts
  - apps/desktop/tsconfig.json
  - apps/desktop/src/main.tsx
  - apps/desktop/src/lib/cubism/bootstrap.ts
  - apps/desktop/src/lib/cubism/webgl-context.ts
  - public/live2dcubismcore.min.js
  - public/live2dcubismcore.wasm
  - src/vendor/cubism/Framework/src/*
  - public/Framework/Shaders/WebGL/*
autonomous: true
requirements: [L2D-01]
user_setup:
  - service: "Live2D Cubism 5 Web SDK"
    why: "Core rendering engine for Live2D .moc3 models"
    download_url: "https://www.live2d.com/en/sdk/download/web/"
    files_needed:
      - "Cubism SDK for Web (full package, contains Framework/src/ and Shaders/)"
      - "live2dcubismcore.min.js + live2dcubismcore.wasm (Core)"

must_haves:
  truths:
    - "Cubism 5 WebGL SDK 文件已部署到项目正确位置"
    - "CubismFramework 在应用启动时成功初始化（startUp + initialize）"
    - "WebView 中可创建 WebGL 上下文（alpha/premultipliedAlpha 透明背景）"
    - "Core WASM 文件在应用加载前已就绪"
    - "Vite 开发和构建流程正确处理 @framework 别名"
  artifacts:
    - path: "public/live2dcubismcore.min.js"
      provides: "Cubism Core JS (含 WASM loader)"
    - path: "src/vendor/cubism/Framework/src/cubismdefaultparameterid.ts"
      provides: "Cubism Framework SDK 入口模块"
    - path: "public/Framework/Shaders/WebGL/vert.shader"
      provides: "WebGL vertex shader (Cubism renderer 依赖)"
    - path: "src/lib/cubism/bootstrap.ts"
      provides: "CubismFramework.startUp/initialize"
    - path: "src/lib/cubism/webgl-context.ts"
      provides: "WebGL 上下文创建工具函数"
    - path: "apps/desktop/vite.config.mts"
      provides: "@framework Vite 别名配置"
    - path: "apps/desktop/tsconfig.json"
      provides: "@framework/* TypeScript paths"
  key_links:
    - from: "main.tsx"
      to: "public/live2dcubismcore.min.js"
      via: "loadScript() → <script> 注入"
      pattern: "loadScript.*live2dcubismcore"
    - from: "bootstrap.ts"
      to: "@framework/live2dcubismframework"
      via: "import → CubismFramework.startUp() + initialize()"
    - from: "vite.config.mts"
      to: "src/vendor/cubism/Framework/src/"
      via: "resolve.alias['@framework']"
    - from: "tsconfig.json"
      to: "src/vendor/cubism/Framework/src/*"
      via: "compilerOptions.paths['@framework/*']"
---

<objective>
Cubism 5 WebGL SDK 集成 — 下载 SDK 文件、配置构建工具、初始化 CubismFramework、部署 Core WASM。

Purpose: 为 Live2DRenderer 提供底层 SDK 基础设施。在此计划完成后，TypeScript 编译链路打通，CubismFramework 可在 WebView 中初始化，WebGL 上下文可创建。

Output: SDK 文件部署、构建配置、Framework bootstrap 模块、WebGL 工具函数、Core WASM 动态加载
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-live2d-core/01-CONTEXT.md

# 旧 Live2D 实现（参考）：
# git show a32c13c~1:apps/desktop/src/main.tsx — Core WASM 动态加载模式
# git show a32c13c~1:apps/desktop/vite.config.mts — 旧 @framework 别名

<interfaces>
<!-- 当前 vite.config.mts 已有别名模式，照此扩展 @framework -->
<!-- 当前 tsconfig.json compilerOptions.paths 已有 @/ @pet-action-dsl @shared 模式，照此添加 @framework/* -->

From apps/desktop/vite.config.mts (现有):
```typescript
resolve: {
  alias: {
    '@': path.resolve(__dirname, 'src'),
    '@pet-action-dsl': path.resolve(__dirname, '../../packages/pet-action-dsl/src'),
    '@shared': path.resolve(__dirname, '../../packages/shared/src'),
  },
},
```

From apps/desktop/tsconfig.json (现有 paths):
```json
"paths": {
  "@/*": ["src/*"],
  "@pet-action-dsl": ["../../packages/pet-action-dsl/src"],
  "@shared": ["../../packages/shared/src"]
}
```

From apps/desktop/src/main.tsx (现有 bootstrap 模式):
```typescript
async function bootstrap(): Promise<void> {
  const baseUrl = window.location.href;
  if (baseUrl.startsWith('http')) {
    console.log('[Rive] Using auto WASM (dev mode)');
  }
  await import('./styles.css');
  const { default: App } = await import('./App');
  // ...
}
bootstrap().catch(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 下载 Cubism 5 WebGL SDK 并部署到项目目录</name>
  <files>
    public/live2dcubismcore.min.js
    public/live2dcubismcore.wasm
    src/vendor/cubism/Framework/src/*         # SDK Framework 源码
    public/Framework/Shaders/WebGL/*          # Shader 文件
  </files>

  <action>
    下载 Live2D Cubism 5 WebGL SDK 文件到项目目录。按以下优先级尝试自动化下载：

    步骤 A — 尝试自动化下载：
    1. 使用 curl 尝试下载 Core:
       ```bash
       curl -fSL "https://cubism.live2d.com/sdk-web/cubismcore/live2dcubismcore.min.js" \
         -o apps/desktop/public/live2dcubismcore.min.js
       curl -fSL "https://cubism.live2d.com/sdk-web/cubismcore/live2dcubismcore.wasm" \
         -o apps/desktop/public/live2dcubismcore.wasm
       ```
    2. 下载 Cubism 5 Web SDK 完整包:
       - 尝试抓取 https://www.live2d.com/en/sdk/download/web/ 页面解析下载链接
       - 或用已知直接 URL 尝试（常见模式: cubismsdkweb-5-r-{X}.zip）
       - 使用 curl 下载并解压到临时目录
    
    步骤 B — 如果自动化失败，创建 checkpoint:human-action 动态提示用户手动下载:
       - 告知用户需要从 https://www.live2d.com/en/sdk/download/web/ 下载 Cubism 5 Web SDK
       - 提供下载后解压到临时目录的步骤

    步骤 C — 部署:
    1. 将 Core 文件复制到 apps/desktop/public/:
       ```bash
       cp {tmpdir}/Core/live2dcubismcore.min.js apps/desktop/public/
       cp {tmpdir}/Core/live2dcubismcore.wasm apps/desktop/public/
       ```
       （如果已通过直接 URL 下载，文件已经在 apps/desktop/public/）

    2. 创建 Framework 目录:
       ```bash
       mkdir -p apps/desktop/src/vendor/cubism/Framework/src
       ```
       从 SDK 包提取 Framework/src/ 所有文件到该目录。

    3. 部署 Shader 文件:
       ```bash
       mkdir -p apps/desktop/public/Framework/Shaders/WebGL
       cp -r {tmpdir}/Framework/src/rendering/*.shader apps/desktop/public/Framework/Shaders/WebGL/
       ```
       （注意：Cubism 5 SDK 的 shader 文件可能在 Framework/src/rendering/ 或 Framework/Shaders/WebGL/ 下，
       根据实际 SDK 目录结构调整。目标：所有 .vert.shader / .frag.shader 文件在 public/Framework/Shaders/WebGL/ 下）

    4. 验证文件完整性:
       - Core JS + WASM: apps/desktop/public/live2dcubismcore.min.js + .wasm
       - Framework 核心模块: src/vendor/cubism/Framework/src/cubismdefaultparameterid.ts
       - Shader 文件: public/Framework/Shaders/WebGL/ 下至少 2 个 .shader 文件
  </action>

  <verify>
    <automated>
      test -f apps/desktop/public/live2dcubismcore.min.js \
        && test -f apps/desktop/public/live2dcubismcore.wasm \
        && test -f apps/desktop/src/vendor/cubism/Framework/src/cubismdefaultparameterid.ts \
        && ls apps/desktop/public/Framework/Shaders/WebGL/*.shader 2>/dev/null | wc -l | awk '{print $1 >= 2}'
    </automated>
  </verify>

  <done>
    Core JS + WASM 在 public/ 中，Framework 源码在 src/vendor/cubism/Framework/src/ 中，
    Shader 文件在 public/Framework/Shaders/WebGL/ 中，文件完整性通过验证
  </done>
</task>

<task type="auto">
  <name>Task 2: 配置 Vite @framework 别名 + TypeScript paths + CubismFramework bootstrap 模块</name>
  <files>
    apps/desktop/vite.config.mts
    apps/desktop/tsconfig.json
    apps/desktop/src/lib/cubism/bootstrap.ts
  </files>

  <action>
    per D-07: 恢复 @framework Vite 别名 → src/vendor/cubism/Framework/src/

    第 1 步 — vite.config.mts:
    在 resolve.alias 中添加 @framework:
    ```typescript
    '@framework': path.resolve(__dirname, 'src/vendor/cubism/Framework/src'),
    ```

    第 2 步 — tsconfig.json:
    在 compilerOptions.paths 中添加:
    ```json
    "@framework/*": ["src/vendor/cubism/Framework/src/*"]
    ```

    第 3 步 — 创建 apps/desktop/src/lib/cubism/bootstrap.ts:
    创建目录（如不存在）: src/lib/cubism/
    实现 ensureCubismFramework():
    ```typescript
    import { CubismFramework, LogLevel, Option } from '@framework/live2dcubismframework';

    let frameworkReady = false;

    /**
     * 确保 CubismFramework 已初始化。
     * 必须在 Core WASM 加载完成后调用。
     * 安全地幂等调用（每个应用生命周期只初始化一次）。
     */
    export function ensureCubismFramework(): void {
      if (frameworkReady) return;
      
      const option = new Option();
      option.logFunction = (message: string) => console.log(`[Cubism] ${message}`);
      option.loggingLevel = LogLevel.LogLevel_Warning;
      
      CubismFramework.startUp(option);
      CubismFramework.initialize();
      frameworkReady = true;
      
      console.log('[Cubism] Framework initialized successfully');
    }
    ```
    参考旧实现模式（git show a32c13c~1），但使用 TypeScript 严格模式（无 any）。
    注意：Cubism 5 SDK 的 API 签名可能略有变化，需要根据实际 SDK 版本微调
    （例如 Option 构造参数、LogLevel 枚举值等）。如果 API 不匹配，按实际 SDK 类型定义调整。
  </action>

  <verify>
    <automated>
      grep -q "@framework" apps/desktop/vite.config.mts \
        && grep -q "@framework/*" apps/desktop/tsconfig.json \
        && test -f apps/desktop/src/lib/cubism/bootstrap.ts
    </automated>
  </verify>

  <done>
    Vite 别名和 TypeScript paths 配置完成，bootstrap.ts 导出 ensureCubismFramework()
  </done>
</task>

<task type="auto">
  <name>Task 3: Core WASM 动态加载 + WebGL 上下文工具函数</name>
  <files>
    apps/desktop/src/main.tsx
    apps/desktop/src/lib/cubism/webgl-context.ts
  </files>

  <action>
    第 1 步 — main.tsx Core WASM 动态加载:
    参考旧实现模式（git show a32c13c~1:apps/desktop/src/main.tsx），在 bootstrap() 函数开头添加
    live2dcubismcore.min.js 的加载。使用已有的 loadScript 模式:

    ```typescript
    async function bootstrap(): Promise<void> {
      // 加载 Live2D Cubism Core WASM（必须在一切之前）
      await loadScript('./live2dcubismcore.min.js');
      console.log('[Cubism] Core WASM loaded');
      
      // ... 现有 bootstrap 逻辑 ...
    }
    ```

    注意：
    - loadScript() 已有防重复加载逻辑（检查 script[src="..."] 是否存在）
    - 保留现有 Rive 相关日志（`[Rive] Using auto WASM (dev mode)`），Phase 4 再移除
    - 移除以注释形式存在的旧 `[Rive] Using auto WASM` 无用代码？保留或删除均可（agent's discretion）

    第 2 步 — 创建 src/lib/cubism/webgl-context.ts:
    创建 WebGL 上下文创建工具函数，包含 per D-06（单 WebGL context，性能考虑）的约束:

    ```typescript
    export interface WebGLContextOptions {
      alpha?: boolean;
      antialias?: boolean;
      premultipliedAlpha?: boolean;
      preserveDrawingBuffer?: boolean;
      stencil?: boolean;
    }

    const DEFAULT_GL_OPTIONS: WebGLContextOptions = {
      alpha: true,
      antialias: true,
      premultipliedAlpha: true,
      preserveDrawingBuffer: true,
      stencil: true,
    };

    /**
     * 在指定 canvas 上创建 WebGL 上下文。
     * 每个 canvas 只创建一个 context（单 context 约束 per D-06）。
     * 返回的 context 已配置 Live2D 渲染所需的 blend 模式。
     */
    export function createWebGLContext(
      canvas: HTMLCanvasElement,
      options?: WebGLContextOptions
    ): WebGLRenderingContext | null {
      const opts = { ...DEFAULT_GL_OPTIONS, ...options };
      const gl = canvas.getContext('webgl', opts) as WebGLRenderingContext | null;
      if (!gl) {
        console.error('[Cubism] Failed to create WebGL context');
        return null;
      }
      // 配置初始 GL 状态（Live2D 渲染所需）
      gl.enable(gl.BLEND);
      gl.blendFunc(gl.ONE, gl.ONE_MINUS_SRC_ALPHA);
      gl.clearColor(0, 0, 0, 0);
      console.log('[Cubism] WebGL context created');
      return gl;
    }
    ```
  </action>

  <verify>
    <automated>
      grep -q "loadScript.*live2dcubismcore" apps/desktop/src/main.tsx \
        && test -f apps/desktop/src/lib/cubism/webgl-context.ts
    </automated>
  </verify>

  <done>
    main.tsx 在 App 渲染前加载 live2dcubismcore.min.js，webgl-context.ts 导出 createWebGLContext()
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK files → WebView | Cubism 5 SDK 文件从外部来源下载后，在本地 WebView 中作为 JS/WASM 执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-L2D-01 | T | SDK 下载 | mitigate | 仅从 live2d.com 官方 URL 下载，不使用第三方 CDN。integrity: 在计划中验证文件存在性，不做 checksum 强制校验（官方不提供） |
| T-L2D-02 | T | Core WASM 执行 | accept | live2dcubismcore.wasm 加载于 WebView 沙箱内，仅执行图形渲染，无网络/文件访问权限 |
| T-L2D-03 | T | Shader 加载 | accept | Shader 文件为纯文本 .shader 格式，同域加载，无外部注入路径 |
</threat_model>

<verification>
- Task 1: SDK 文件已部署（file existence checks pass）
- Task 2: vite.config.mts + tsconfig.json 配置正确，bootstrap.ts 可编译
- Task 3: main.tsx Core 加载逻辑正确，webgl-context.ts 导出可用
</verification>

<success_criteria>
1. ✓ Cubism 5 SDK 文件部署到项目（Core JS/WASM、Framework 源码、Shaders）
2. ✓ @framework Vite 别名可用，TypeScript paths 可解析
3. ✓ CubismFramework.startUp() + initialize() 在 bootstrap 模块中可调用
4. ✓ main.tsx 在 App 渲染前加载 Core WASM
5. ✓ WebGL 上下文创建工具函数可用（alpha + premultipliedAlpha）
</success_criteria>

<output>
After completion, create `.planning/phases/01-live2d-core/01-01-SUMMARY.md`
</output>
