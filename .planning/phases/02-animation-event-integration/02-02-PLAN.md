---
phase: 02-animation-event-integration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [apps/desktop/src/features/pet/RiveRenderer.ts]
autonomous: true
requirements: [SYNC-02, SYNC-03]
must_haves:
  truths:
    - 'TTS 播报时 mouth_open 输入跟随 RMS 振幅变化，嘴型动画同步'
    - '鼠标在宠物上移动时，宠物视线平滑跟随'
    - '鼠标移出宠物后 resetPointer 将视线恢复居中'
    - '低于 0.05 的振幅被削波，嘴巴保持闭合无抖动'
    - 'look_x/look_y 在 -1.0~1.0 范围内平滑插值'
  artifacts:
    - path: apps/desktop/src/features/pet/RiveRenderer.ts
      provides: TTS 嘴型同步、鼠标跟随、render loop 平滑插值
      contains: setSpeaking, lookAt, resetPointer, startRenderLoop, updateMouseFollow, updateLipSync, currentAmplitude, currentLookX, currentLookY
  key_links:
    - from: RiveRenderer.setSpeaking
      to: mouthOpenInput.value
      via: 振幅削波后通过 rAF 平滑设置
      pattern: 'this\.mouthOpenInput\.value = '
    - from: RiveRenderer.lookAt
      to: targetLookX/targetLookY
      via: 归一化画布坐标 → SM 空间 -1.0~1.0
      pattern: 'this\.targetLook'
    - from: RiveRenderer rAF loop
      to: updateMouseFollow() + updateLipSync()
      via: 每帧更新 lerp 值并设置 SM 输入
      pattern: 'updateMouseFollow\|updateLipSync'
---

<objective>
为 RiveRenderer 实现 TTS 嘴型同步 (SYNC-02) 和鼠标跟随 (SYNC-03)。

**目的:** TTS 播报时 `mouth_open` 输入跟随 RMS 振幅变化（0.05 削波迟滞），实现嘴型动画同步；鼠标在宠物上移动时 `look_x`/`look_y` 输入平滑跟随（lerp 插值），实现视线追踪。

**输出:** 修改后的 `RiveRenderer.ts`，实现 setSpeaking()、lookAt()、resetPointer() 和 rAF 平滑循环。
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-animation-event-integration/02-CONTEXT.md
@.planning/phases/02-animation-event-integration/02-RESEARCH.md
@.planning/phases/02-animation-event-integration/02-01-PLAN.md
@apps/desktop/src/features/pet/RiveRenderer.ts
@apps/desktop/src/features/pet/PetRenderer.ts
@apps/desktop/src/features/pet/rive-inputs.ts
@apps/desktop/src/stores/pet-store.ts
@apps/desktop/src/audio/streaming-player.ts
@apps/desktop/src/components/PetStage.tsx
@apps/desktop/src/App.tsx

<interfaces>
From apps/desktop/src/features/pet/PetRenderer.ts:
```typescript
// 这两个方法 Plan 02 要实现:
setSpeaking(speaking: boolean, amplitude?: number): void;
lookAt(x: number, y: number): void;
resetPointer(): void;
```

From apps/desktop/src/components/PetStage.tsx (调用方):
```typescript
// 嘴型同步调用点 (lines 263-271):
useEffect(() => {
  if (!modelLoaded || !rendererRef.current) return;
  if (isSpeaking) {
    rendererRef.current.setSpeaking(true, ttsAmplitude);
  } else {
    rendererRef.current.setSpeaking(false, 0);
  }
}, [isSpeaking, ttsAmplitude, modelLoaded]);

// 鼠标跟随调用点 (lines 226-249):
// rendererRef.current?.lookAt(canvasX, canvasY);
// 每 50ms setInterval, canvasX/canvasY 是画布像素坐标
```

From apps/desktop/src/audio/streaming-player.ts:
```typescript
// RMS 振幅来源 (lines 175-197):
// rms 范围: 0.0~1.0
// 通过 PetStore → React re-render → PetStage → setSpeaking 管道传输
```

从 Plan 01 添加的字段和方法（执行此 Plan 前必须存在）:
```typescript
private stateInput: StateMachineInput | null = null;
private mouthOpenInput: StateMachineInput | null = null;
private lookXInput: StateMachineInput | null = null;
private lookYInput: StateMachineInput | null = null;

private cacheInputs(rive: Rive, smName: string): void { ... }
private idleTimerId: number | null = null;
private clearIdleTimer(): void { ... }
public scheduleIdle(delay: number): void { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 TTS 嘴型同步 — setSpeaking() + 振幅平滑</name>
  <read_first>
    @apps/desktop/src/features/pet/RiveRenderer.ts (Plan 01 执行后的完整文件)
    @apps/desktop/src/features/pet/rive-inputs.ts (RIVE_INPUTS.MOUTH_OPEN)
    @apps/desktop/src/audio/streaming-player.ts (RMS 范围)
    @apps/desktop/src/stores/pet-store.ts (ttsAmplitude 字段)
  </read_first>
  <files>apps/desktop/src/features/pet/RiveRenderer.ts</files>
  <action>
    1. 在类字段区域（stateInput 等缓存字段之后、idleTimerId 附近）添加以下字段:

    ```typescript
    // TTS 嘴型同步 (SYNC-02)
    private isSpeaking = false;
    private currentAmplitude = 0;
    private targetAmplitude = 0;
    private lastMouthValue = 0;
    private readonly AMPLITUDE_CLAMP = 0.05;    // D-07: 削波阈值
    private readonly AMP_LERP_FACTOR = 0.2;     // 振幅平滑系数
    private mouthCurrentlyOpen = false;          // 削波迟滞状态
    private readonly AMP_HYSTERESIS = 0.02;      // 迟滞带宽度
    ```

    2. 实现 `setSpeaking()` 方法（替换第 101-102 行的空壳）:

    ```typescript
    // D-06: 直接设置 mouth_open number 输入
    // D-07: 低于 0.05 的 RMS 忽略
    // D-08: 0.0~1.0 直接透传
    setSpeaking(speaking: boolean, amplitude = 0): void {
      this.isSpeaking = speaking;
      if (speaking) {
        // D-07: 振幅削波
        this.targetAmplitude = amplitude > this.AMPLITUDE_CLAMP
          ? Math.min(amplitude, 1.0)  // D-08: 范围 0.0~1.0
          : 0;
      } else {
        this.targetAmplitude = 0;
      }
    }
    ```

    3. 添加 `updateLipSync()` 方法（放在 `setSpeaking()` 之后）:

    ```typescript
    /** 在 rAF 循环中每帧调用: 平滑振幅并设置 mouth_open SM 输入 */
    private updateLipSync(): void {
      // 振幅平滑 (指数移动平均)
      this.currentAmplitude += (this.targetAmplitude - this.currentAmplitude) * this.AMP_LERP_FACTOR;

      if (!this.mouthOpenInput) return;

      // 迟滞削波 (防止在阈值附近快速开关)
      if (this.mouthCurrentlyOpen) {
        // 已开口: 降到 0.03 以下才关闭
        const mouthValue = this.currentAmplitude < this.AMPLITUDE_CLAMP - this.AMP_HYSTERESIS
          ? 0 : this.currentAmplitude;
        this.mouthCurrentlyOpen = mouthValue > 0;
        if (Math.abs(mouthValue - this.lastMouthValue) > 0.005) {
          this.mouthOpenInput.value = mouthValue;
          this.lastMouthValue = mouthValue;
          console.log(`[RiveRenderer] mouth_open → ${mouthValue.toFixed(3)}`);
        }
      } else {
        // 已闭口: 超过 0.05 才开口
        const mouthValue = this.currentAmplitude > this.AMPLITUDE_CLAMP
          ? this.currentAmplitude : 0;
        this.mouthCurrentlyOpen = mouthValue > 0;
        if (Math.abs(mouthValue - this.lastMouthValue) > 0.005) {
          this.mouthOpenInput.value = mouthValue;
          this.lastMouthValue = mouthValue;
          if (mouthValue > 0) {
            console.log(`[RiveRenderer] mouth_open → ${mouthValue.toFixed(3)}`);
          }
        }
      }
    }
    ```
  </action>
  <behavior>
    - Given: isSpeaking=true, amplitude=0.3 → targetAmplitude=0.3 (超过 0.05 削波)
    - Given: isSpeaking=true, amplitude=0.03 → targetAmplitude=0 (低于 0.05 削波)
    - Given: isSpeaking=false → targetAmplitude=0
    - Given: 振幅在 0.04~0.06 间抖动 → 迟滞防止快速开关
  </behavior>
  <verify>
    <automated>grep -n "setSpeaking" apps/desktop/src/features/pet/RiveRenderer.ts | head -1 | grep -q "boolean.*amplitude" && grep -n "updateLipSync" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "private updateLipSync" && grep -n "AMPLITUDE_CLAMP" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "0\.05"</automated>
  </verify>
  <acceptance_criteria>
    - grep "private isSpeaking = false" RiveRenderer.ts → 匹配
    - grep "private currentAmplitude = 0" RiveRenderer.ts → 匹配
    - grep "private readonly AMPLITUDE_CLAMP = 0\.05" RiveRenderer.ts → 匹配
    - grep "private readonly AMP_HYSTERESIS = 0\.02" RiveRenderer.ts → 匹配
    - grep "setSpeaking(speaking: boolean, amplitude = 0)" RiveRenderer.ts → 匹配
    - grep "this\.targetAmplitude = " RiveRenderer.ts → 匹配
    - grep "private updateLipSync" RiveRenderer.ts → 匹配
    - grep "this\.mouthOpenInput\.value = " RiveRenderer.ts → 匹配
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>
    - setSpeaking() 实现: 设置 isSpeaking 和 targetAmplitude，振幅低于 0.05 削波为 0
    - updateLipSync() 实现: 振幅平滑 lerp + 迟滞削波 + 变化超过 0.005 时设置 mouthOpenInput
    - 所有振幅相关字段已声明
    - TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现鼠标跟随 — lookAt() + resetPointer() + 坐标归一化</name>
  <read_first>
    @apps/desktop/src/features/pet/RiveRenderer.ts (Task 1 修改后的完整文件)
    @apps/desktop/src/components/PetStage.tsx (lines 226-249: 鼠标跟随调用方式)
  </read_first>
  <files>apps/desktop/src/features/pet/RiveRenderer.ts</files>
  <action>
    1. 在类字段区域（振幅字段附近）添加:

    ```typescript
    // 鼠标跟随 (SYNC-03)
    private targetLookX = 0;
    private targetLookY = 0;
    private currentLookX = 0;
    private currentLookY = 0;
    private lastLookX = 999;   // 确保首次设置生效
    private lastLookY = 999;
    private readonly LOOK_LERP_FACTOR = 0.1;   // D-09: lerp 平滑系数
    ```

    2. 实现 `lookAt()` 方法（替换第 107-108 行的空壳）:

    ```typescript
    // D-11: 画布像素坐标 → SM 空间 -1.0~1.0
    // D-09: 设置 lerp 目标值，实际插值在 rAF 循环中完成
    lookAt(x: number, y: number): void {
      const canvas = this.mainCanvas;
      if (!canvas) return;

      // D-11: 归一化: canvas pixel → -1.0..1.0
      this.targetLookX = (x / canvas.width) * 2 - 1;
      this.targetLookY = (y / canvas.height) * 2 - 1;

      // 削波到 SM 期望范围
      this.targetLookX = Math.max(-1, Math.min(1, this.targetLookX));
      this.targetLookY = Math.max(-1, Math.min(1, this.targetLookY));
    }
    ```

    3. 实现 `resetPointer()` 方法（替换第 110-111 行的空壳）:

    ```typescript
    // 将宠物视线恢复居中（鼠标跟随禁用时调用）
    resetPointer(): void {
      this.targetLookX = 0;
      this.targetLookY = 0;
    }
    ```

    4. 添加 `updateMouseFollow()` 方法:

    ```typescript
    /** 在 rAF 循环中每帧调用: lerp 平滑并设置 look_x/look_y SM 输入 */
    private updateMouseFollow(): void {
      // D-09: 指数平滑 (lerp)
      this.currentLookX += (this.targetLookX - this.currentLookX) * this.LOOK_LERP_FACTOR;
      this.currentLookY += (this.targetLookY - this.currentLookY) * this.LOOK_LERP_FACTOR;

      // 仅在变化显著时更新 SM 输入（避免冗余 WASM 调用）
      if (this.lookXInput && Math.abs(this.currentLookX - this.lastLookX) > 0.001) {
        this.lookXInput.value = this.currentLookX;
        this.lastLookX = this.currentLookX;
      }
      if (this.lookYInput && Math.abs(this.currentLookY - this.lastLookY) > 0.001) {
        this.lookYInput.value = this.currentLookY;
        this.lastLookY = this.currentLookY;
      }
    }
    ```
  </action>
  <behavior>
    - Given: canvas 宽 520, 高 760; lookAt(100, 200) → targetLookX = 100/520*2-1 = -0.615, targetLookY = 200/760*2-1 = -0.474
    - Given: currentLookX=0, targetLookX=-0.615 → 每帧 lerp: current += (-0.615 - current) * 0.1
    - Given: resetPointer() → targetLookX=0, targetLookY=0
    - Given: 连续 lookAt 值接近 (< 0.001) → 不更新 SM 输入
  </behavior>
  <verify>
    <automated>grep -n "lookAt" apps/desktop/src/features/pet/RiveRenderer.ts | grep -v "//" | head -1 | grep -q "canvas.*width" && grep -n "resetPointer" apps/desktop/src/features/pet/RiveRenderer.ts | grep -v "//" | grep -q "targetLookX = 0"</automated>
  </verify>
  <acceptance_criteria>
    - grep "private targetLookX = 0" RiveRenderer.ts → 匹配
    - grep "private currentLookX = 0" RiveRenderer.ts → 匹配
    - grep "private readonly LOOK_LERP_FACTOR = 0\.1" RiveRenderer.ts → 匹配
    - grep "lookAt.*number.*number" RiveRenderer.ts → 匹配（完整签名）
    - grep "x / canvas.width" RiveRenderer.ts → 匹配（归一化）
    - grep "Math.max(-1, Math.min(1, " RiveRenderer.ts → 匹配（削波）
    - grep "resetPointer.*void" RiveRenderer.ts → 匹配
    - grep "private updateMouseFollow" RiveRenderer.ts → 匹配
    - grep "this\.lookXInput\.value = this\.currentLookX" RiveRenderer.ts → 匹配
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>
    - lookAt() 实现: 画布像素归一化为 -1.0~1.0 + 削波
    - resetPointer() 实现: targetLookX/targetLookY 归零
    - updateMouseFollow() 实现: lerp 平滑 + 变化 > 0.001 时更新 SM 输入
    - TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 启动 rAF 渲染循环 — 整合 lerp 更新 + lip sync</name>
  <read_first>
    @apps/desktop/src/features/pet/RiveRenderer.ts (Task 2 修改后的完整文件)
    @apps/desktop/src/features/pet/rive-inputs.ts (所有常量)
  </read_first>
  <files>apps/desktop/src/features/pet/RiveRenderer.ts</files>
  <action>
    1. 在类字段区域添加:

    ```typescript
    // rAF 渲染循环
    private rafId = 0;
    ```

    2. 添加 `startRenderLoop()` 和 `stopRenderLoop()` 方法:

    ```typescript
    /** 开始 rAF 渲染循环: 每帧更新 SM 输入平滑值 */
    private startRenderLoop(): void {
      if (this.rafId) return; // 防止重复启动
      const loop = () => {
        if (this.disposed) return;
        this.updateMouseFollow();
        this.updateLipSync();
        this.rafId = requestAnimationFrame(loop);
      };
      this.rafId = requestAnimationFrame(loop);
    }

    /** 停止 rAF 渲染循环 */
    private stopRenderLoop(): void {
      if (this.rafId) {
        cancelAnimationFrame(this.rafId);
        this.rafId = 0;
      }
    }
    ```

    3. 在 `loadModel()` 的 onLoad 回调末尾（设置 stateInput.value = 0 之后）添加:

    ```typescript
    this.startRenderLoop();
    ```

    4. 更新 `destroy()` 方法，在清理前停止渲染循环:

    ```typescript
    destroy(): void {
      this.disposed = true;
      this.stopRenderLoop();
      this.clearIdleTimer();
      this.cleanupInstances();
      this.mainCanvas = null;
      this.bgCanvas = null;
      console.log('[RiveRenderer] Destroyed');
    }
    ```
  </action>
  <behavior>
    - Given: onLoad 完成 → startRenderLoop() 被调用 → rAF 循环运行
    - Given: destroy() 被调用 → stopRenderLoop() 被调用 → cancelAnimationFrame(rafId)
    - 每帧: updateMouseFollow() + updateLipSync() 被按序调用
  </behavior>
  <verify>
    <automated>grep -n "private startRenderLoop" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "startRenderLoop" && grep -n "private stopRenderLoop" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "stopRenderLoop" && grep -n "this\.startRenderLoop()" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "onLoad" && grep -n "this\.stopRenderLoop()" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "destroy"</automated>
  </verify>
  <acceptance_criteria>
    - grep "private rafId = 0" RiveRenderer.ts → 匹配
    - grep "private startRenderLoop" RiveRenderer.ts → 匹配
    - grep "this\.updateMouseFollow" RiveRenderer.ts | grep "loop" → 匹配
    - grep "this\.updateLipSync" RiveRenderer.ts | grep "loop" → 匹配
    - grep "this\.startRenderLoop()" RiveRenderer.ts → 匹配
    - grep "this\.stopRenderLoop()" RiveRenderer.ts → 匹配
    - destroy 中顺序: stopRenderLoop → clearIdleTimer → cleanupInstances
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>
    - startRenderLoop() 在每个 onLoad 回调末尾被调用
    - rAF 循环每帧调用 updateMouseFollow() 和 updateLipSync()
    - destroy() 中先停止 rAF 循环再清理实例
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — Phase 2 is animation-only. All data (mouse coordinates, RMS amplitude) comes from within the Electron app's own process.

## Threat Register

No security concerns — animation-only phase. All inputs are internal (same-process mouse events, audio RMS). No external data, no network requests, no user text input.

Key observations:
- `lookAt()` receives canvas pixel coordinates from PetStage (internal, same-process)
- `setSpeaking()` receives RMS float from StreamingAudioPlayer (internal, same-process)
- SM inputs are set on Rive WASM instance only (no network or storage)
- Console logging is debug-only, no sensitive data exposure
</threat_model>

<verification>
1. `npx tsc --noEmit` must pass in `apps/desktop/`
2. grep verifications: all acceptance_criteria patterns must match
3. Manual verification (D-16):
   - TTS lip sync: enable TTS, speak text, observe mouth animation
   - Mouse follow: move cursor over pet, observe smooth eye tracking
   - Reset pointer: disable mouse follow, observe pet eyes return to center
</verification>

<success_criteria>
- TTS 播报时 mouthOpenInput.value 跟随 RMS 振幅，0.05 削波 + 迟滞防止抖动
- 鼠标移动时 look_x/look_y 以 lerp(0.1) 平滑追踪
- 鼠标移出/禁用跟随: resetPointer() 将 targetLookX/Y 归零
- rAF 循环正确启动（onLoad）和停止（destroy）
- 变化 < 0.005(mouth) / 0.001(look) 时不重复设置 SM 输入
- 所有 console.log 输出符合 D-17 要求
</success_criteria>

<output>
After completion, create `.planning/phases/02-animation-event-integration/02-02-SUMMARY.md`
</output>
