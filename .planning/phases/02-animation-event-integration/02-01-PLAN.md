---
phase: 02-animation-event-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/desktop/src/features/pet/RiveRenderer.ts]
autonomous: true
requirements: [SYNC-01, SYNC-04, SYNC-05]
must_haves:
  truths:
    - 'Agent 事件（thinking/happy/error 等）正确映射到 Rive SM 的 state 输入'
    - '瞬间动作（happy/error）播完后自动回到 idle'
    - '新动作能打断当前动画，立即切换到新 state'
  artifacts:
    - path: apps/desktop/src/features/pet/RiveRenderer.ts
      provides: SM input 缓存、playAction 打断逻辑、idle 自动返回
      contains: cacheInputs, scheduleIdle, clearIdleTimer, stateInput, RIVE_STATE_INDEX, MOMENTARY_ACTIONS
  key_links:
    - from: RiveRenderer.loadModel
      to: cacheInputs()
      via: onLoad callback
      pattern: 'this\.cacheInputs\('
    - from: RiveRenderer.playAction
      to: stateInput.value
      via: 直接设置缓存的 stateInput
      pattern: 'this\.stateInput\.value'
    - from: playAction (momentary action)
      to: scheduleIdle()
      via: idle 定时器
      pattern: 'scheduleIdle\('
---

<objective>
为 RiveRenderer 建立基础架构：缓存 SM 输入引用、实现动作中断和 idle 自动返回。

**目的:** 将 `setRiveStateInputs()` 的 O(n) 每次搜索模式替换为 O(1) 缓存输入访问，为后续嘴型同步和鼠标跟随打下基础。同时实现动作中断（SYNC-05）和 idle 自动返回（SYNC-04），使 Agent 事件能正确驱动 SM state。

**输出:** 修改后的 `RiveRenderer.ts`，包含缓存输入、动作中断、idle 定时器。
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-animation-event-integration/02-CONTEXT.md
@.planning/phases/02-animation-event-integration/02-RESEARCH.md
@apps/desktop/src/features/pet/RiveRenderer.ts
@apps/desktop/src/features/pet/PetRenderer.ts
@apps/desktop/src/features/pet/rive-inputs.ts

<interfaces>
From apps/desktop/src/features/pet/PetRenderer.ts:
```typescript
export interface PlayActionOptions {
  playback?: 'hold' | 'restart' | 'momentary';
}
```

From apps/desktop/src/features/pet/rive-inputs.ts:
```typescript
export const RIVE_INPUTS = {
  STATE: 'state',
  BLINK_TRIGGER: 'blink',
  BREATHE_TRIGGER: 'breathe',
  MOUTH_OPEN: 'mouth_open',
  LOOK_X: 'look_x',
  LOOK_Y: 'look_y',
} as const;

export type RiveStateValue = 'idle' | 'thinking' | 'speaking' | 'happy' | 'error'
  | 'searching' | 'coding' | 'terminal' | 'confused' | 'angry';
```

Existing RiveRenderer fields (line 12-17):
```typescript
private instances: RiveInstance[] = [];
private mainCanvas: HTMLCanvasElement | null = null;
private bgCanvas: HTMLCanvasElement | null = null;
private currentAction: string = 'idle';
private disposed = false;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 SM 输入缓存系统 — cacheInputs() + 缓存字段</name>
  <read_first>
    @apps/desktop/src/features/pet/RiveRenderer.ts (lines 1-60: import, interface, 类字段, loadModel onLoad)
    @apps/desktop/src/features/pet/rive-inputs.ts (全部: RIVE_INPUTS 常量定义)
  </read_first>
  <files>apps/desktop/src/features/pet/RiveRenderer.ts</files>
  <action>
    1. 在 RiveRenderer 类的 `private disposed = false` 之后（第 17 行后）添加以下缓存字段:

    ```typescript
    // 缓存 SM 输入引用（由 cacheInputs 在 onLoad 时填充）
    private stateInput: StateMachineInput | null = null;
    private mouthOpenInput: StateMachineInput | null = null;
    private lookXInput: StateMachineInput | null = null;
    private lookYInput: StateMachineInput | null = null;
    ```

    2. 在 `private actionToState()` 方法之前添加 `cacheInputs()` 方法:

    ```typescript
    private cacheInputs(rive: Rive, smName: string): void {
      const inputs = rive.stateMachineInputs(smName);
      console.log('[RiveRenderer] Available SM inputs:', inputs?.map(i => `${i.name} (${StateMachineInputType[i.type]})`));
      if (!inputs) return;
      for (const input of inputs) {
        if (input.name === RIVE_INPUTS.STATE) {
          this.stateInput = input;
        } else if (input.name === RIVE_INPUTS.MOUTH_OPEN) {
          this.mouthOpenInput = input;
        } else if (input.name === RIVE_INPUTS.LOOK_X) {
          this.lookXInput = input;
        } else if (input.name === RIVE_INPUTS.LOOK_Y) {
          this.lookYInput = input;
        }
      }
      console.log('[RiveRenderer] Cached inputs — state:', !!this.stateInput, 'mouth:', !!this.mouthOpenInput, 'lookX:', !!this.lookXInput, 'lookY:', !!this.lookYInput);
    }
    ```

    3. 修改 `loadModel()` 中的 `onLoad` 回调（第 50-53 行），将 `this.setRiveStateInputs(charRive, smName, 'idle')` 替换为:

    ```typescript
    onLoad: () => {
      charRive.resizeToCanvas();
      this.cacheInputs(charRive, smName);
      // 初始化 state 为 idle(0)
      if (this.stateInput) {
        this.stateInput.value = 0;
      }
    },
    ```

    4. 从 `setRiveStateInputs()` 方法（第 149-170 行）中移除对 `RIVE_INPUTS.STATE` number input 的处理。保留 trigger 和 boolean 输入的处理逻辑（用于 blink/breathe trigger），但移除 state number 的 .find() 搜索逻辑，因为现在使用缓存的 stateInput 引用。

    修改后的 `setRiveStateInputs` 只处理 trigger 和 boolean 输入（移除 number type 中 state 的处理分支）:

    ```typescript
    private setRiveStateInputs(rive: Rive, smName: string, stateValue: RiveStateValue): void {
      const inputs = rive.stateMachineInputs(smName);
      for (const input of inputs) {
        if (input.type === StateMachineInputType.Trigger) {
          if (input.name === stateValue) {
            input.fire();
          }
        } else if (input.type === StateMachineInputType.Boolean) {
          if (input.name === stateValue) {
            (input as StateMachineInput).value = true;
          }
        }
      }
    }
    ```
  </action>
  <behavior>
    - Given: loadModel 完成 onLoad → cacheInputs 被调用 → stateInput 不为 null
    - Given: stateInput.value 在 onLoad 中被设置为 0
  </behavior>
  <verify>
    <automated>grep -n "private stateInput" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "StateMachineInput | null" && grep -n "private cacheInputs" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "private cacheInputs" && grep -n "this\.cacheInputs(" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "onLoad"</automated>
  </verify>
  <acceptance_criteria>
    - grep "private stateInput: StateMachineInput | null = null" RiveRenderer.ts → 匹配
    - grep "private mouthOpenInput: StateMachineInput | null = null" RiveRenderer.ts → 匹配
    - grep "private lookXInput: StateMachineInput | null = null" RiveRenderer.ts → 匹配
    - grep "private cacheInputs" RiveRenderer.ts → 匹配
    - grep "this\.cacheInputs(" RiveRenderer.ts → 匹配
    - grep "RIVE_INPUTS.STATE" RiveRenderer.ts | grep "this\.stateInput" → 匹配（缓存赋值）
    - grep "stateInput.value = 0" RiveRenderer.ts → 匹配（初始化idle）
    - `npx tsc --noEmit` 通过（TypeScript 编译验证）
  </acceptance_criteria>
  <done>
    - cacheInputs() 方法存在且被 onLoad 调用
    - 四个 SM 输入字段（stateInput, mouthOpenInput, lookXInput, lookYInput）声明为类字段
    - onLoad 中设置了 stateInput.value = 0
    - setRiveStateInputs 移除了 state number 搜索逻辑
    - TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现动作中断和 idle 自动返回 — playAction 重构 + idle 定时器</name>
  <read_first>
    @apps/desktop/src/features/pet/RiveRenderer.ts (Task 1 修改后的完整文件)
    @apps/desktop/src/features/pet/PetRenderer.ts (PlayActionOptions 接口)
    @apps/desktop/src/features/pet/rive-inputs.ts (RiveStateValue 类型和 RIVE_STATES)
  </read_first>
  <files>apps/desktop/src/features/pet/RiveRenderer.ts</files>
  <action>
    执行以下修改:

    1. 在类顶部（`private disposed = false` 附近，或在 cached inputs 字段之后）添加:

    ```typescript
    // Idle 自动返回定时器 (D-02/D-04)
    private idleTimerId: number | null = null;
    private readonly MOMENTARY_ACTIONS = new Set(['happy', 'error', 'clicked', 'doubleClicked', 'wake']);
    private readonly RIVE_STATE_INDEX: Record<string, number> = {
      idle: 0, thinking: 1, speaking: 2, happy: 3, error: 4,
      searching: 5, coding: 6, terminal: 7, confused: 8, angry: 9,
    };
    ```

    2. 在 `cacheInputs()` 方法之后、`actionToState()` 之前添加:

    ```typescript
    /** D-04: 公开 scheduleIdle 供外部调用覆盖默认定时 */
    scheduleIdle(delay: number): void {
      this.clearIdleTimer();
      this.idleTimerId = window.setTimeout(() => {
        this.idleTimerId = null;
        if (this.stateInput) {
          this.stateInput.value = 0; // idle
          console.log('[RiveRenderer] Idle auto-return triggered');
        }
      }, delay);
    }

    private clearIdleTimer(): void {
      if (this.idleTimerId !== null) {
        window.clearTimeout(this.idleTimerId);
        this.idleTimerId = null;
      }
    }
    ```

    3. 重写 `playAction()` 方法（第 83-89 行）为:

    ```typescript
    async playAction(actionName: string, options?: PlayActionOptions): Promise<void> {
      // D-12: 立即打断 — 先清除任何待处理的 idle 返回
      this.clearIdleTimer();

      this.currentAction = actionName;

      // D-01: 通过缓存的 stateInput 直接设置 state number
      const stateValue = this.actionToState(actionName);
      const stateIndex = this.RIVE_STATE_INDEX[stateValue] ?? 0;
      if (this.stateInput) {
        this.stateInput.value = stateIndex;
        console.log(`[RiveRenderer] Action: ${actionName} → state ${stateIndex} (${stateValue})`); // D-17
      }

      // D-02: 瞬间动作 → 设置 idle 自动返回定时器
      // D-03: 持续动作 → 不设置定时器，等待外部触发
      const isMomentary = options?.playback === 'momentary' || this.MOMENTARY_ACTIONS.has(actionName);
      if (isMomentary) {
        this.scheduleIdle(400); // 默认 400ms，agent's discretion
      }
    }
    ```

    **注意:** 同时移除 `playAction()` 中原本遍历 `this.instances` 调用 `setRiveStateInputs` 的循环。新的 `playAction()` 使用缓存的 `this.stateInput` 直接在 primary instance 上设置。不需要遍历所有 instances，因为 SM state 是全局的，background 不需要同步 state。

    4. 更新 `idle()` 方法（第 132-134 行），使其也能清除 idle 定时器:

    ```typescript
    idle(): void {
      this.clearIdleTimer();
      this.playAction('idle');
    }
    ```
  </action>
  <behavior>
    - Given: idleTimerId is null → playAction('happy') 被调用 → stateInput.value = 3 → setIdleTimer(400ms)
    - Given: idleTimer is pending → playAction('thinking') 被调用 → idleTimer 被清除 → stateInput.value = 1
    - Given: idleTimer fires → stateInput.value = 0
  </behavior>
  <verify>
    <automated>grep -n "private idleTimerId" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "number | null" && grep -n "scheduleIdle(" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "clearIdleTimer" && grep -n "this\.MOMENTARY_ACTIONS" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "has"</automated>
  </verify>
  <acceptance_criteria>
    - grep "private idleTimerId: number | null = null" RiveRenderer.ts → 匹配
    - grep "private readonly MOMENTARY_ACTIONS" RiveRenderer.ts → 匹配（含 happy, error, clicked, doubleClicked, wake）
    - grep "private readonly RIVE_STATE_INDEX" RiveRenderer.ts → 匹配（含 idle=0 到 angry=9）
    - grep "scheduleIdle(delay: number)" RiveRenderer.ts → 匹配
    - grep "clearIdleTimer()" RiveRenderer.ts → 匹配
    - grep "clearIdleTimer" RiveRenderer.ts | grep "playAction" → 匹配（playAction 中调用 clearIdleTimer）
    - grep "this\.stateInput\.value = stateIndex" RiveRenderer.ts → 匹配（直接设置 cached stateInput）
    - grep "MOMENTARY_ACTIONS\.has" RiveRenderer.ts → 匹配
    - grep "scheduleIdle(400)" RiveRenderer.ts → 匹配
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>
    - idleTimerId 字段及 scheduleIdle/clearIdleTimer 方法存在
    - playAction() 中: 先 clearIdleTimer → 设置 stateInput.value → momentary 动作设置 400ms 定时器
    - playAction() 不再遍历 instances 调用 setRiveStateInputs（使用缓存 stateInput）
    - idle() 也调用 clearIdleTimer
    - TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 添加调试日志基础设施 (D-17)</name>
  <read_first>
    @apps/desktop/src/features/pet/RiveRenderer.ts (Task 2 修改后的完整文件)
  </read_first>
  <files>apps/desktop/src/features/pet/RiveRenderer.ts</files>
  <action>
    在 `loadModel()` 的 onLoad 回调中添加 SM 输入名称和类型日志。修改 onLoad 为:

    ```typescript
    onLoad: () => {
      charRive.resizeToCanvas();
      console.log('[RiveRenderer] Model loaded, caching SM inputs...');
      this.cacheInputs(charRive, smName);
      if (this.stateInput) {
        this.stateInput.value = 0;
        console.log('[RiveRenderer] Initial state → idle(0)');
      }
    },
    ```

    **确保所有 D-17 要求的日志点都已覆盖:**
    - cacheInputs() 已经打印 `Available SM inputs` 和 `Cached inputs — state:/mouth:/lookX:/lookY:`（见 Task 1）
    - playAction() 已经打印 `Action: ${actionName} → state ${stateIndex} (${stateValue})`（见 Task 2）
    - scheduleIdle 已经打印 `Idle auto-return triggered`（见 Task 2）
    - 添加 model loaded 日志如上
    - 确保 `destroy()` 方法打印 `[RiveRenderer] Destroyed`:

    修改 `destroy()` 方法（第 136-141 行），添加日志:

    ```typescript
    destroy(): void {
      this.disposed = true;
      this.clearIdleTimer();
      this.cleanupInstances();
      this.mainCanvas = null;
      this.bgCanvas = null;
      console.log('[RiveRenderer] Destroyed');
    }
    ```
  </action>
  <behavior>
    - onLoad 执行时会看到控制台输出 "[RiveRenderer] Model loaded, caching SM inputs..."
    - cacheInputs 执行时会看到 Available SM inputs 列表
    - playAction 执行时会看到 state 变化日志
  </behavior>
  <verify>
    <automated>grep -n "Model loaded, caching" apps/desktop/src/features/pet/RiveRenderer.ts | grep -q "console.log" && grep -n "clearIdleTimer()" apps/desktop/src/features/pet/RiveRenderer.ts | grep "destroy" | grep -q "clearIdleTimer"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Model loaded, caching SM inputs" RiveRenderer.ts → 匹配
    - grep "Initial state → idle(0)" RiveRenderer.ts → 匹配
    - grep "RiveRenderer.*Destroyed" RiveRenderer.ts → 匹配
    - grep "console.log.*RiveRenderer" RiveRenderer.ts | wc -l ≥ 6（至少 6 处日志点）
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <done>
    - onLoad 中打印 model loaded 日志
    - cacheInputs 中打印可用输入列表（Task 1 已添加）
    - playAction 中打印 state 变化（Task 2 已添加）
    - scheduleIdle 中打印 idle auto-return（Task 2 已添加）
    - destroy 中打印 Destroyed 日志并调用 clearIdleTimer
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — Phase 2 is animation-only. All data (mouse coordinates, RMS amplitude, action names) comes from within the Electron app's own process.

## Threat Register

No security concerns — animation-only phase. All inputs are internal (same-process mouse events, audio RMS, action enums). No external data, no network requests, no user text input.
</threat_model>

<verification>
1. `npx tsc --noEmit` must pass in `apps/desktop/`
2. grep verifications: all acceptance_criteria patterns must match
3. Manual smoke test: load dev server (`npm run dev:renderer`), check console for SM input logs
</verification>

<success_criteria>
- SM inputs cached on load (stateInput, mouthOpenInput, lookXInput, lookYInput)
- playAction() sets state via cached input, not per-frame .find()
- Momentary actions (happy/error/clicked/doubleClicked/wake) auto-return to idle after ~400ms
- Sustained actions (thinking/speaking) stay until external trigger
- New playAction clears pending idle timer (interrupt works)
- Console logs at all key points: load, cache, action change, idle auto-return, destroy
</success_criteria>

<output>
After completion, create `.planning/phases/02-animation-event-integration/02-01-SUMMARY.md`
</output>
