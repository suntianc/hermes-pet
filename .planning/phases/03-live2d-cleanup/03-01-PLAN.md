---
phase: 03-live2d-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/vendor/cubism/ (DELETE)
  - apps/desktop/src/vendor/live2dcubismcore.d.ts (DELETE)
  - apps/desktop/public/live2dcubismcore.js (DELETE)
  - apps/desktop/public/Framework/ (DELETE)
  - apps/desktop/public/models/ (DELETE)
  - apps/desktop/src/features/pet/Live2DRenderer.ts (DELETE)
  - apps/desktop/src/features/pet/capability-resolver.ts (DELETE)
  - apps/desktop/src/features/pet-performance/pet-performance-director.ts (DELETE)
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "src/vendor/cubism/ 目录不存在"
    - "src/vendor/live2dcubismcore.d.ts 不存在"
    - "public/live2dcubismcore.js 不存在"
    - "public/Framework/ 目录不存在"
    - "public/models/ 目录下无 Live2D 模型文件"
    - "Live2DRenderer.ts 不存在"
    - "capability-resolver.ts 不存在"
    - "pet-performance-director.ts 不存在"
  artifacts:
    - path: "apps/desktop/src/vendor/cubism/"
      provides: "Cubism 5 Framework SDK（~59 .ts 文件）"
      deleted: true
    - path: "apps/desktop/src/vendor/live2dcubismcore.d.ts"
      provides: "Cubism Core 类型定义"
      deleted: true
    - path: "apps/desktop/public/live2dcubismcore.js"
      provides: "Cubism Core WASM 加载器（247KB）"
      deleted: true
    - path: "apps/desktop/public/Framework/"
      provides: "WebGL shader 文件（13 .frag/.vert）"
      deleted: true
    - path: "apps/desktop/public/models/"
      provides: "Live2D 模型文件（~50 文件：.moc3, .model3.json, .motion3.json 等）"
      deleted: true
    - path: "apps/desktop/src/features/pet/Live2DRenderer.ts"
      provides: "Cubism 5 WebGL 渲染器（891 行）"
      deleted: true
    - path: "apps/desktop/src/features/pet/capability-resolver.ts"
      provides: "动作→动画组映射（55 行）"
      deleted: true
    - path: "apps/desktop/src/features/pet-performance/pet-performance-director.ts"
      provides: "GSAP 动画管理器（被 Rive SM 替代，D-16）"
      deleted: true
  key_links:
    - from: "Live2DRenderer.ts"
      to: "@framework 别名"
      via: "import from @framework/*"
      pattern: "@framework/"
    - from: "capability-resolver.ts"
      to: "ModelConfig 类型"
      via: "import ModelConfig from model-registry"
    - from: "public/models/"
      to: "models.json 运行时 fetch"
      via: "loadModelConfigs() 读取路径"
    - from: "pet-performance-director.ts"
      to: "App.tsx"
      via: "import PetPerformanceDirector in App.tsx"
      status: "被 03-03 修复引用"
    - from: "pet-performance-director.ts"
      to: "package.json (gsap)"
      via: "import gsap from 'gsap'"
      status: "删除后 gsap 不再被使用，03-03 卸载依赖"
---

<objective>
删除所有 Live2D 文件和目录 — 大爆炸删除。

Per D-01 (big bang strategy)，ALL Live2D-related files and directories are deleted in one wave, including the PetPerformanceDirector (D-16). Nothing to build, nothing to compile — pure deletion. After this plan, the project will have TypeScript compilation errors (expected — Plan 02 & 03 fix them).

**Purpose:** 物理删除所有不再使用的 Live2D/Cubism 5 代码、资源文件，以及被 Rive SM 替代的 GSAP 动画管理器。
**Output:** 8 组目录/文件被 git rm 删除，dangling 引用等待 Plan 02 & 03 修复。
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live2d-cleanup/03-CONTEXT.md
@.planning/phases/03-live2d-cleanup/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Cubism SDK vendor directories + Cubism Core files</name>

  <read_first>
  N/A — pure deletion, no file reading needed
  </read_first>

  <files>
  (deleted)
  - apps/desktop/src/vendor/cubism/ (~59 files)
  - apps/desktop/src/vendor/live2dcubismcore.d.ts
  - apps/desktop/public/live2dcubismcore.js
  - apps/desktop/public/Framework/ (13 shader files)
  </files>

  <action>
  使用 `git rm -rf` 删除以下目录和文件（D-03, D-04, D-05, D-06）：

  ```bash
  # D-03: 删除 Cubism 5 Framework SDK（~59 .ts 文件，~22K 行）
  git rm -rf apps/desktop/src/vendor/cubism/

  # D-04: 删除 Cubism Core 类型定义（15KB .d.ts）
  git rm apps/desktop/src/vendor/live2dcubismcore.d.ts

  # D-05: 删除 Cubism Core WASM 加载器（247KB JS）
  git rm apps/desktop/public/live2dcubismcore.js

  # D-06: 删除 WebGL shader 文件（13 files: 6 .vert + 7 .frag）
  git rm -rf apps/desktop/public/Framework/
  ```

  使用 `git rm -rf` 确保 git 索引同步删除。不要使用 `rm -rf` 后手动 `git add` —— `git rm -rf` 一步到位。
  </action>

  <acceptance_criteria>
  ```bash
  # 所有以下路径应返回 "No such file or directory"
  ls apps/desktop/src/vendor/cubism/ 2>&1 | grep -q "No such file"
  ls apps/desktop/src/vendor/live2dcubismcore.d.ts 2>&1 | grep -q "No such file"
  ls apps/desktop/public/live2dcubismcore.js 2>&1 | grep -q "No such file"
  ls apps/desktop/public/Framework/ 2>&1 | grep -q "No such file"
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  for f in \
    "apps/desktop/src/vendor/cubism/" \
    "apps/desktop/src/vendor/live2dcubismcore.d.ts" \
    "apps/desktop/public/live2dcubismcore.js" \
    "apps/desktop/public/Framework/"; do
    if ls $f 2>/dev/null; then echo "FAIL: $f still exists"; exit 1; fi
  done
  echo "PASS: All Task 1 files deleted"
  </automated>
  </verify>

  <done>
  - src/vendor/cubism/ 不存在（CLEAN-01）
  - src/vendor/live2dcubismcore.d.ts 不存在
  - public/live2dcubismcore.js 不存在（CLEAN-02）
  - public/Framework/ 不存在（CLEAN-02）
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete Live2D model files + Live2D renderer + PetPerformanceDirector</name>

  <read_first>
  N/A — pure deletion, no file reading needed
  </read_first>

  <files>
  (deleted)
  - apps/desktop/public/models/ (~50 files: Jian + Vivian model files)
  - apps/desktop/src/features/pet/Live2DRenderer.ts
  - apps/desktop/src/features/pet/capability-resolver.ts
  - apps/desktop/src/features/pet-performance/pet-performance-director.ts
  </files>

  <action>
  使用 `git rm -rf` 删除以下目录和文件（D-07, D-08, D-09, D-16）：

  ```bash
  # D-07: 删除所有 Live2D 模型文件（Jian ~23 files + Vivian ~27 files）
  # 包括 .moc3, .model3.json, .motion3.json, .exp3.json, .physics3.json, .cdi3.json, textures (.png)
  git rm -rf apps/desktop/public/models/

  # D-08: 删除 Cubism 5 WebGL 渲染器（891 行）
  git rm apps/desktop/src/features/pet/Live2DRenderer.ts

  # D-09: 删除 Capability Resolver（55 行）
  git rm apps/desktop/src/features/pet/capability-resolver.ts

  # D-16: 删除 PetPerformanceDirector（GSAP 动画管理器 — 被 Rive SM 替代）
  # 删除此文件后 gsap 不再被任何代码使用，03-03 将从 package.json 卸载 gsap
  git rm apps/desktop/src/features/pet-performance/pet-performance-director.ts
  ```

  注意：public/models/ 包含约 50 个文件（Jian 23 + Vivian 27）。删除 pet-performance-director.ts 后 gsap 再无使用者，03-03 卸载 gsap + extract-zip 依赖。
  </action>

  <acceptance_criteria>
  ```bash
  # 验证文件已删除
  ls apps/desktop/public/models/ 2>&1 | grep -q "No such file"
  ls apps/desktop/src/features/pet/Live2DRenderer.ts 2>&1 | grep -q "No such file"
  ls apps/desktop/src/features/pet/capability-resolver.ts 2>&1 | grep -q "No such file"
  ls apps/desktop/src/features/pet-performance/pet-performance-director.ts 2>&1 | grep -q "No such file"

  # 确认 git 状态 — 应有大量已删除文件 staged
  git status --short | grep -c "D "
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  ERR=0
  for f in \
    "apps/desktop/public/models/" \
    "apps/desktop/src/features/pet/Live2DRenderer.ts" \
    "apps/desktop/src/features/pet/capability-resolver.ts" \
    "apps/desktop/src/features/pet-performance/pet-performance-director.ts"; do
    if ls "$f" 2>/dev/null; then echo "FAIL: $f still exists"; ERR=1; fi
  done
  if [ $ERR -eq 0 ]; then
    echo "PASS: All Task 2 files deleted"
    echo "Deleted files count: $(git diff --cached --name-only --diff-filter=D | wc -l | tr -d ' ')"
  fi
  exit $ERR
  </automated>
  </verify>

  <done>
  - public/models/ 不包含任何 Live2D 模型文件（CLEAN-03）
  - Live2DRenderer.ts 不存在（CLEAN-04）
  - capability-resolver.ts 不存在（CLEAN-04）
  - pet-performance-director.ts 不存在（D-16，gsap 使用方已删除）
  - git index 中所有删除文件已 staged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure file deletion phase — no new code, no data flow, no trust boundaries introduced or modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | N/A | N/A | accept | Phase 3 is a cleanup phase — no new code, no network access, no data processing, no authentication. Security posture unchanged. |
</threat_model>

<verification>
```bash
# 验证所有 8 组路径已删除
echo "=== Verification: All deleted paths ==="
for p in \
  "apps/desktop/src/vendor/cubism/" \
  "apps/desktop/src/vendor/live2dcubismcore.d.ts" \
  "apps/desktop/public/live2dcubismcore.js" \
  "apps/desktop/public/Framework/" \
  "apps/desktop/public/models/" \
  "apps/desktop/src/features/pet/Live2DRenderer.ts" \
  "apps/desktop/src/features/pet/capability-resolver.ts" \
  "apps/desktop/src/features/pet-performance/pet-performance-director.ts"; do
  if ls "$p" 2>/dev/null; then echo "❌ STILL EXISTS: $p"; else echo "✅ DELETED: $p"; fi
done
```
</verification>

<success_criteria>
- [ ] src/vendor/cubism/ 目录删除（CLEAN-01）
- [ ] src/vendor/live2dcubismcore.d.ts 删除（CLEAN-01）
- [ ] public/live2dcubismcore.js 删除（CLEAN-02）
- [ ] public/Framework/ 目录删除（CLEAN-02）
- [ ] public/models/ 目录删除（CLEAN-03）
- [ ] Live2DRenderer.ts 删除（CLEAN-04）
- [ ] capability-resolver.ts 删除（CLEAN-04）
- [ ] pet-performance-director.ts 删除（D-16）
- [ ] git index 中所有删除文件已 staged、可提交
</success_criteria>

<output>
After completion, create `.planning/phases/03-live2d-cleanup/03-01-SUMMARY.md`
</output>
