---
phase: 03-live2d-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/desktop/vite.config.mts
  - apps/desktop/tsconfig.json
  - apps/desktop/src/features/pet/model-registry.ts
  - apps/desktop/src/features/pet/PetRenderer.ts
  - apps/desktop/src/main.tsx
autonomous: true
requirements:
  - CLEAN-05
  - CLEAN-06

must_haves:
  truths:
    - "vite.config.mts 中无 @framework 别名"
    - "tsconfig.json 中无 @framework/* 路径映射"
    - "ModelType 联合类型中无 'live2d'"
    - "PetRendererType 联合类型中无 'live2d'"
    - "main.tsx 中无 live2dcubismcore.js 的 loadScript 调用"
    - "FALLBACK_MODELS 为空数组（不引用已删除的 .model3.json 路径）"
    - "npm run build 编译零错误"
    - "grep 搜索无 Live2D/Cubism 源码引用残留"
  artifacts:
    - path: "apps/desktop/vite.config.mts"
      provides: "Vite 构建配置"
      modified: true
      changes: "删除 @framework 别名行"
    - path: "apps/desktop/tsconfig.json"
      provides: "TypeScript 编译配置"
      modified: true
      changes: "删除 @framework/* paths 条目"
    - path: "apps/desktop/src/features/pet/model-registry.ts"
      provides: "ModelType 定义 + FALLBACK_MODELS"
      modified: true
      changes: "'live2d' 从 ModelType 移除；FALLBACK_MODELS 置空"
    - path: "apps/desktop/src/features/pet/PetRenderer.ts"
      provides: "PetRendererType 定义"
      modified: true
      changes: "'live2d' 从 PetRendererType 移除"
    - path: "apps/desktop/src/main.tsx"
      provides: "应用入口"
      modified: true
      changes: "移除 live2dcubismcore.js WASM 动态加载 + loadScript 函数"
  key_links:
    - from: "vite.config.mts @framework 别名"
      to: "src/vendor/cubism/ (已删除)"
      pattern: "@framework.*vendor/cubism"
      status: "删除别名引用"
    - from: "tsconfig.json paths"
      to: "src/vendor/cubism/* (已删除)"
      pattern: "@framework/*"
      status: "删除路径映射"
    - from: "ModelType 'live2d'"
      to: "ModelConfig.type"
      via: "联合类型成员"
      status: "从联合类型移除"
    - from: "PetRendererType 'live2d'"
      to: "RendererConfig.type"
      via: "联合类型成员"
      status: "从联合类型移除"
    - from: "main.tsx loadScript"
      to: "public/live2dcubismcore.js (已删除)"
      via: "动态 script 加载"
      status: "删除加载调用"
---

<objective>
修复 Vite/TypeScript 编译配置和源文件中的 Live2D 引用，使渲染进程编译通过。

After Plan 01 deleted all Live2D files, this plan fixes the dangling references in configuration files and source code. The TypeScript compiler will catch any missed references via `npm run build`.

**Purpose:** 处理因大爆炸删除产生的编译错误。移除 @framework 别名和路径映射、清理类型定义中 'live2d' 值、移除 WASM 动态加载。
**Output:** 5 个文件修改完成，`npm run build` 编译通过，grep 确认无 Live2D 源码引用残留。
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live2d-cleanup/03-CONTEXT.md
@.planning/phases/03-live2d-cleanup/03-RESEARCH.md

<interfaces>
<!-- Key types referenced in this plan -->

From apps/desktop/src/features/pet/model-registry.ts (BEFORE):
```typescript
export type ModelType = 'live2d' | 'rive';  // → 'rive'
export interface ModelConfig {
  id: string;
  name: string;
  path: string;
  type?: ModelType;
  // ...
}
```

From apps/desktop/src/features/pet/PetRenderer.ts (BEFORE):
```typescript
export type PetRendererType = 'live2d' | 'spine' | 'gif' | 'vrm';  // → 'spine' | 'gif' | 'vrm'
export interface RendererConfig {
  type: PetRendererType;
  modelPath: string;
  autoFit?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove @framework alias from vite.config.mts and tsconfig.json</name>

  <read_first>
  - apps/desktop/vite.config.mts
  - apps/desktop/tsconfig.json
  </read_first>

  <files>
  - apps/desktop/vite.config.mts
  - apps/desktop/tsconfig.json
  </files>

  <action>
  修改两个配置文件，移除 @framework 别名（D-10, D-11）。

  **vite.config.mts：**
  删除包含 `'@framework'` 的别名行。编辑后 alias 对象只保留：
  ```typescript
  alias: {
    '@': path.resolve(__dirname, 'src'),
    '@pet-action-dsl': path.resolve(__dirname, '../../packages/pet-action-dsl/src'),
    '@shared': path.resolve(__dirname, '../../packages/shared/src'),
  },
  ```

  **tsconfig.json：**
  删除包含 `"@framework/*"` 的 paths 条目。编辑后 paths 对象只保留：
  ```json
  "paths": {
    "@/*": ["src/*"],
    "@pet-action-dsl": ["../../packages/pet-action-dsl/src"],
    "@shared": ["../../packages/shared/src"]
  }
  ```

  操作方式：使用 `edit` 工具删除包含 `'@framework'` / `"@framework/*"` 的行。
  </action>

  <acceptance_criteria>
  ```bash
  # @framework 不应再出现在配置文件中
  grep -c "@framework" apps/desktop/vite.config.mts && echo "EXPECT 0"
  grep -c "@framework" apps/desktop/tsconfig.json && echo "EXPECT 0"
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  grep -rn "@framework" apps/desktop/vite.config.mts apps/desktop/tsconfig.json \
    && { echo "FAIL: @framework still found in config"; exit 1; } \
    || echo "PASS: @framework removed from config"
  </automated>
  </verify>

  <done>
  - vite.config.mts: @framework 别名已删除，保留 @、@pet-action-dsl、@shared
  - tsconfig.json: @framework/* 路径映射已删除，保留其他 paths
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove 'live2d' from ModelType and PetRendererType + clean FALLBACK_MODELS</name>

  <read_first>
  - apps/desktop/src/features/pet/model-registry.ts
  - apps/desktop/src/features/pet/PetRenderer.ts
  </read_first>

  <files>
  - apps/desktop/src/features/pet/model-registry.ts
  - apps/desktop/src/features/pet/PetRenderer.ts
  </files>

  <action>
  修改两个类型定义文件和清理 FALLBACK_MODELS（D-13, D-14 + agent's discretion per CONTEXT.md）。

  **1. model-registry.ts（D-13）：**

  a) ModelType 联合类型：
  - Before: `export type ModelType = 'live2d' | 'rive';`
  - After:  `export type ModelType = 'rive';`

  b) FALLBACK_MODELS 常量：FALLBACK_MODELS 中的 Jian 条目引用 `path: 'models/Jian/Jian.model3.json'`，该路径随 public/models/ 删除而消失。清空该数组。
  - Before: `export const FALLBACK_MODELS: ModelConfig[] = [ { id: 'jian', ... } ];`
  - After:  `export const FALLBACK_MODELS: ModelConfig[] = [];`

  **2. PetRenderer.ts（D-14）：**

  PetRendererType：
  - Before: `export type PetRendererType = 'live2d' | 'spine' | 'gif' | 'vrm';`
  - After:  `export type PetRendererType = 'spine' | 'gif' | 'vrm';`
  </action>

  <acceptance_criteria>
  ```bash
  grep -c "'live2d'" apps/desktop/src/features/pet/model-registry.ts && echo "EXPECT 0"
  grep -c "'live2d'" apps/desktop/src/features/pet/PetRenderer.ts && echo "EXPECT 0"
  grep -c "model3.json" apps/desktop/src/features/pet/model-registry.ts && echo "EXPECT 0"
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  ERR=0
  if grep -q "'live2d'" apps/desktop/src/features/pet/model-registry.ts; then
    echo "FAIL: 'live2d' still in model-registry.ts"; ERR=1
  fi
  if grep -q "'live2d'" apps/desktop/src/features/pet/PetRenderer.ts; then
    echo "FAIL: 'live2d' still in PetRenderer.ts"; ERR=1
  fi
  if grep -q "model3.json" apps/desktop/src/features/pet/model-registry.ts; then
    echo "FAIL: .model3.json still referenced in model-registry.ts"; ERR=1
  fi
  if [ $ERR -eq 0 ]; then echo "PASS: All type values cleaned"; fi
  exit $ERR
  </automated>
  </verify>

  <done>
  - ModelType: "'live2d'" 已移除，只保留 "'rive'"
  - PetRendererType: "'live2d'" 已移除，只保留 "'spine' | 'gif' | 'vrm'"
  - FALLBACK_MODELS: 清空为空数组（不再引用已删除的 .model3.json 路径）
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove Cubism Core WASM dynamic loading from main.tsx</name>

  <read_first>
  - apps/desktop/src/main.tsx
  </read_first>

  <files>
  - apps/desktop/src/main.tsx
  </files>

  <action>
  移除 main.tsx 中的 Live2D WASM 动态加载（D-12 + agent's discretion on loadScript）。

  **修改 1 — 删除 loadScript 函数（agent's discretion）：**
  移除 Cubism 加载后，`loadScript` 函数不再被调用。Rive WASM 由 @rive-app/canvas 自动管理。删除整个 `function loadScript(src: string): Promise<void> { ... }` 函数定义。

  **修改 2 — 移除 loadScript 调用：**
  - Before: `  await loadScript('./live2dcubismcore.js');`
  - After:  删除该行

  **修改 3 — 调整 bootstrap 函数签名（如 loadScript 是函数体内唯一内容）：**
  如果 bootstrap 函数在移除 loadScript 调用后只剩 `const baseUrl = ...` 等行，确保函数格式正确。
  </action>

  <acceptance_criteria>
  ```bash
  grep -c "loadScript" apps/desktop/src/main.tsx && echo "EXPECT 0"
  grep -c "live2dcubismcore" apps/desktop/src/main.tsx && echo "EXPECT 0"
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  ERR=0
  if grep -q "loadScript" apps/desktop/src/main.tsx; then
    echo "FAIL: loadScript still in main.tsx"; ERR=1
  fi
  if grep -q "live2dcubismcore" apps/desktop/src/main.tsx; then
    echo "FAIL: live2dcubismcore still in main.tsx"; ERR=1
  fi
  if [ $ERR -eq 0 ]; then echo "PASS: main.tsx cleaned"; fi

  # 编译验证 — 使用 npm run build:renderer 进行快速增量检查
  echo "--- Build verification (renderer-only) ---"
  npm run build:renderer 2>&1
  BUILD_EXIT=$?
  if [ $BUILD_EXIT -ne 0 ]; then echo "FAIL: renderer build failed"; ERR=1; fi
  exit $ERR
  </automated>
  </verify>

  <done>
  - main.tsx 中无 loadScript('./live2dcubismcore.js') 调用
  - main.tsx 中无 loadScript 函数定义
  - bootstrap 函数不再包含 WASM 加载步骤
  - npm run build:renderer 编译零错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Configuration and type definition cleanup — no new code, no data flow, no trust boundaries introduced or modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | N/A | N/A | accept | Configuration/type cleanup only — no runtime security impact. Removing unused aliases reduces attack surface marginally. |
</threat_model>

<verification>
```bash
echo "=== Verification: Plan 03-02 ==="

# 1. Config clean
echo "--- Config: @framework removal ---"
grep -rn "@framework" apps/desktop/vite.config.mts apps/desktop/tsconfig.json \
  && { echo "❌ FAIL: @framework found"; exit 1; } \
  || echo "✅ PASS: @framework removed"

# 2. Type clean
echo "--- Types: 'live2d' removal ---"
grep -rn "'live2d'" apps/desktop/src/features/pet/model-registry.ts apps/desktop/src/features/pet/PetRenderer.ts \
  && { echo "❌ FAIL: 'live2d' found in types"; exit 1; } \
  || echo "✅ PASS: 'live2d' removed from types"

# 3. main.tsx clean
echo "--- main.tsx: live2dcubismcore removal ---"
grep -rn "live2dcubismcore\|loadScript" apps/desktop/src/main.tsx \
  && { echo "❌ FAIL: Live2D reference found in main.tsx"; exit 1; } \
  || echo "✅ PASS: main.tsx cleaned"

# 4. Full build check (D-20)
echo "--- Full TypeScript build: npm run build ---"
npm run build 2>&1
BUILD_EXIT=$?
if [ $BUILD_EXIT -eq 0 ]; then echo "✅ PASS: npm run build succeeds"; else echo "❌ FAIL: build failed"; exit 1; fi

# 5. Grep audit: no Live2D references in source code (D-21)
echo "--- Grep audit: Live2D references ---"
MATCHES=$(grep -rn "live2d\|cubism\|\.moc3\|\.model3\|live2dcubism" \
  --include="*.{ts,tsx,mts}" apps/desktop/src apps/desktop/electron \
  apps/desktop/vite.config.mts apps/desktop/tsconfig.json 2>/dev/null | wc -l | tr -d ' ')
if [ "$MATCHES" -eq 0 ]; then echo "✅ PASS: Zero Live2D source references"; else echo "⚠️  WARNING: $MATCHES Live2D references found (check if legit)"; fi

# 6. dev:renderer smoke test (D-22)
echo "--- dev:renderer smoke test ---"
# Start dev server in background, wait for it to be ready, then kill it
npm run dev:renderer &
DEV_PID=$!
sleep 5
if kill -0 $DEV_PID 2>/dev/null; then
  echo "✅ PASS: dev:renderer started (PID: $DEV_PID)"
  kill $DEV_PID 2>/dev/null
else
  echo "❌ FAIL: dev:renderer failed to start"
  exit 1
fi
```
</verification>

<success_criteria>
- [ ] vite.config.mts: @framework 别名已删除（CLEAN-05）
- [ ] tsconfig.json: @framework/* 路径映射已删除（CLEAN-05）
- [ ] model-registry.ts: ModelType 中 'live2d' 已移除，FALLBACK_MODELS 已清空（D-13）
- [ ] PetRenderer.ts: PetRendererType 中 'live2d' 已移除（D-14）
- [ ] main.tsx: live2dcubismcore.js loadScript 已移除（CLEAN-06）
- [ ] npm run build 编译通过（TypeScript 零错误）（D-20）
- [ ] grep 审计确认无 Live2D/Cubism 源码引用残留（D-21）
- [ ] npm run dev:renderer 启动正常（D-22）
</success_criteria>

<output>
After completion, create `.planning/phases/03-live2d-cleanup/03-02-SUMMARY.md`
</output>
