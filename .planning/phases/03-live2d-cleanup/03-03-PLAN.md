---
phase: 03-live2d-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/desktop/src/App.tsx
  - apps/desktop/public/assets/models/models.json
  - apps/desktop/electron/model-manager.ts
  - apps/desktop/package.json
autonomous: true
requirements:
  - CLEAN-07

must_haves:
  truths:
    - "App.tsx 中无 PetPerformanceDirector 引用"
    - "App.tsx 中无 .live2d-container CSS 类名（已改为 .rive-container）"
    - "public/assets/models/models.json 中无 Live2D 模型条目"
    - "electron/model-manager.ts 中无 extract-zip 导入"
    - "electron/model-manager.ts 中无 .model3.json 文件扫描/校验逻辑"
    - "package.json dependencies 中无 gsap"
    - "package.json dependencies 中无 extract-zip"
    - "npm run build 编译零错误"
  artifacts:
    - path: "apps/desktop/src/App.tsx"
      provides: "应用根组件（~480 行）"
      modified: true
      changes: "移除 PetPerformanceDirector import/ref/useEffect/playPose/playSpeech；.live2d-container → .rive-container"
    - path: "apps/desktop/public/assets/models/models.json"
      provides: "模型注册表（运行时 JSON）"
      modified: true
      changes: "移除 Jian/Vivian Live2D 条目，保留空 models 数组"
    - path: "apps/desktop/electron/model-manager.ts"
      provides: "主进程模型管理"
      modified: true
      changes: "移除 extract-zip import + .model3.json 扫描/校验函数"
    - path: "apps/desktop/package.json"
      provides: "项目依赖配置"
      modified: true
      changes: "移除 gsap + extract-zip 依赖"
  key_links:
    - from: "App.tsx"
      to: "pet-performance-director.ts (已删除)"
      via: "import 语句"
      status: "删除 import + 所有引用处"
    - from: "App.tsx"
      to: "PetStage.tsx .rive-container"
      via: "CSS 类名选择器"
      status: ".live2d-container → .rive-container 保持同步"
    - from: "electron/model-manager.ts"
      to: "extract-zip package"
      via: "import extractZip from 'extract-zip'"
      status: "删除 import + .model3.json 验证路径"
    - from: "package.json"
      to: "gsap (依赖)"
      via: "dependencies 条目"
      status: "卸载（pet-performance-director.ts 已删除）"
---

<objective>
清理 App.tsx、models.json、model-manager.ts 中的 Live2D 残留引用，并移除无用依赖。

This is Wave 2 / Plan 03: after Plan 01 deleted all Live2D files, this plan fixes the remaining source code references in App.tsx (PetPerformanceDirector removal + CSS rename), data files (models.json), main process (model-manager.ts), and package.json (gsap + extract-zip).

**Purpose:** 修复 App.tsx 中对已删除 PetPerformanceDirector 的引用、重命名 CSS 类名保持与 PetStage 一致、清理模型注册表和主进程中的 Live2D 残留、卸载无用 npm 依赖。
**Output:** 4 个文件修改完成，`npm run build` 编译通过。
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live2d-cleanup/03-CONTEXT.md
@.planning/phases/03-live2d-cleanup/03-RESEARCH.md

<interfaces>
<!-- Contracts this plan depends on -->

From apps/desktop/src/App.tsx (required to understand PetPerformanceDirector integration):
- import PetPerformanceDirector
- performanceDirectorRef = useRef<PetPerformanceDirector | null>(null)
- useEffect that creates/disposes PetPerformanceDirector
- playPose() call on currentAction change
- playSpeech() call on bubble/speaking state
- .live2d-container CSS class reference

From apps/desktop/electron/model-manager.ts:
- import extractZip from 'extract-zip' (line 6)
- findPackagedModel3() — .model3.json file scan
- validateModelPackage() — .model3.json validation
- parseModel3References() — .model3.json reference parsing
- importModelZip() — calls extractZip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx — Remove PetPerformanceDirector + rename CSS class</name>

  <read_first>
  - apps/desktop/src/App.tsx
  </read_first>

  <files>
  - apps/desktop/src/App.tsx
  </files>

  <action>
  在 App.tsx 中执行以下修改（D-15）：

  **修改 1 — 删除 import PetPerformanceDirector：**
  删除包含 `import { PetPerformanceDirector }` 或 `from './features/pet-performance/pet-performance-director'` 的 import 行。

  **修改 2 — 删除 performanceDirectorRef：**
  删除 `performanceDirectorRef = useRef<PetPerformanceDirector | null>(null)` 声明。

  **修改 3 — 删除 PetPerformanceDirector 初始化 useEffect：**
  找到初始化 PetPerformanceDirector 的 useEffect（引用 `performanceDirectorRef.current = new PetPerformanceDirector(...)`），删除整个 useEffect 及其依赖数组。

  **修改 4 — 删除 playPose 调用：**
  在监听 `currentAction` 变化的 useEffect 中，找到 `performanceDirectorRef.current?.playPose(...)` 调用并删除。保留 useEffect 中其他逻辑。

  **修改 5 — 删除 playSpeech 调用：**
  在监听 bubble/speaking 状态的 useEffect 中，找到 `performanceDirectorRef.current?.playSpeech(...)` 调用并删除。保留 useEffect 中其他逻辑。

  **修改 6 — CSS 类名重命名：**
  将文件中所有 `.live2d-container` 替换为 `.rive-container`。这需要与 PetStage.tsx 中使用的类名保持一致（PetStage 已在 Phase 1/2 中使用 .rive-container）。

  **验证（每次修改后）：**
  ```bash
  grep -c "PetPerformanceDirector" apps/desktop/src/App.tsx  # 应为 0
  grep -c "performanceDirectorRef" apps/desktop/src/App.tsx  # 应为 0
  grep -c "playPose\|playSpeech" apps/desktop/src/App.tsx     # 仅保留非 PetPerformanceDirector 的 playPose/playSpeech（如有其他调用）
  grep -c "live2d-container" apps/desktop/src/App.tsx        # 应为 0
  grep -c "rive-container" apps/desktop/src/App.tsx          # 应 > 0
  ```
  </action>

  <acceptance_criteria>
  ```bash
  # 无 PetPerformanceDirector 引用
  grep -c "pet-performance-director" apps/desktop/src/App.tsx | grep -q "^0$"
  # CSS 类名已重命名
  grep -c "live2d-container" apps/desktop/src/App.tsx | grep -q "^0$"
  grep -c "rive-container" apps/desktop/src/App.tsx | grep -qv "^0$"
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  ERR=0
  if grep -q "pet-performance-director" apps/desktop/src/App.tsx; then
    echo "FAIL: PetPerformanceDirector import still in App.tsx"; ERR=1
  fi
  if grep -q "performanceDirectorRef" apps/desktop/src/App.tsx; then
    echo "FAIL: performanceDirectorRef still in App.tsx"; ERR=1
  fi
  if grep -q "live2d-container" apps/desktop/src/App.tsx; then
    echo "FAIL: .live2d-container still in App.tsx"; ERR=1
  fi
  if ! grep -q "rive-container" apps/desktop/src/App.tsx; then
    echo "FAIL: .rive-container not found in App.tsx"; ERR=1
  fi
  if [ $ERR -eq 0 ]; then echo "PASS: App.tsx cleaned"; fi
  exit $ERR
  </automated>
  </verify>

  <done>
  - App.tsx: 无 PetPerformanceDirector import/ref/useEffect/playPose/playSpeech
  - App.tsx: 所有 .live2d-container 已改为 .rive-container
  - TypeScript 编译通过（PetPerformanceDirector 引用错误已修复）
  </done>
</task>

<task type="auto">
  <name>Task 2: models.json — Remove Live2D entries + model-manager.ts — Remove extract-zip</name>

  <read_first>
  - apps/desktop/public/assets/models/models.json
  - apps/desktop/electron/model-manager.ts
  </read_first>

  <files>
  - apps/desktop/public/assets/models/models.json
  - apps/desktop/electron/model-manager.ts
  </files>

  <action>
  两个独立的数据/主进程文件修改（D-17, D-18）。

  **A) public/assets/models/models.json（D-17）：**

  读取 models.json，找到 `models` 数组，移除所有现有条目（Jian/Vivian 的 Live2D 模型）。保留空数组以便 Phase 4 填充 .riv 条目。

  Before:
  ```json
  {
    "models": [
      { "id": "jian", "name": "Jian", "path": "models/Jian/Jian.model3.json", ... },
      { "id": "vivian", "name": "Vivian", "path": "models/Vivian/Vivian.model3.json", ... }
    ]
  }
  ```

  After:
  ```json
  {
    "models": []
  }
  ```

  **B) electron/model-manager.ts（D-18）：**

  执行以下修改：

  1. **删除 import extractZip**（line 6）：
     删除 `import extractZip from 'extract-zip';` 行

  2. **删除 .model3.json 扫描/校验函数**：
     - `findPackagedModel3()` — 整个函数，扫描 .model3.json 文件
     - `validateModelPackage()` — 整个函数，验证 .model3.json 包结构
     - `parseModel3References()` — 整个函数，解析 .model3.json 中的引用

  3. **简化 importModelZip() 函数**：
     删除函数体中对 extractZip 的调用和 .model3.json 验证。将该函数替换为返回 null 的桩（placeholder 供 Phase 4 使用）：
     ```typescript
     async function importModelZip(_filePath: string): Promise<string | null> {
       console.warn('[model-manager] .zip import not yet supported for .riv models (Phase 4)');
       return null;
     }
     ```

  4. **保留的结构**：
     - `importModelViaDialog()` — 保留函数签名，Phase 4 适配 .riv
     - `listUserModels()` — 保留
     - `indexBundledModels()` — 保留（model3File 引用后面在 Phase 4 修改）
     - `rendererActionConfig` 类型 — 保留（供 listUserModels 和 indexBundledModels 使用）

  注意：D-18 要求只移除 extract-zip 和 .model3.json 逻辑，保留其他结构供 Phase 4 使用。
  </action>

  <acceptance_criteria>
  ```bash
  # models.json: 无 Live2D 模型条目（无 .model3.json 引用）
  grep -c "\.model3" apps/desktop/public/assets/models/models.json | grep -q "^0$"

  # model-manager.ts: 无 extract-zip import
  grep -c "extract-zip" apps/desktop/electron/model-manager.ts | grep -q "^0$"
  # model-manager.ts: 无 .model3.json 引用
  grep -c "model3" apps/desktop/electron/model-manager.ts | grep -q "^0$"
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  ERR=0

  # Verify models.json
  if grep -q "Jian.model3.json" apps/desktop/public/assets/models/models.json 2>/dev/null; then
    echo "FAIL: Live2D entries still in models.json"; ERR=1
  else
    echo "PASS: models.json cleaned"
  fi

  # Verify model-manager.ts — no extract-zip import
  if grep -q "extract-zip" apps/desktop/electron/model-manager.ts 2>/dev/null; then
    echo "FAIL: extract-zip import still in model-manager.ts"; ERR=1
  else
    echo "PASS: extract-zip removed from model-manager.ts"
  fi

  # Verify model-manager.ts — no .model3.json scan/validation
  if grep -q "findPackagedModel3\|validateModelPackage\|parseModel3References" apps/desktop/electron/model-manager.ts 2>/dev/null; then
    echo "FAIL: .model3.json functions still in model-manager.ts"; ERR=1
  else
    echo "PASS: .model3.json functions removed from model-manager.ts"
  fi

  exit $ERR
  </automated>
  </verify>

  <done>
  - public/assets/models/models.json: models 数组为空（无 Live2D 条目）（D-17）
  - electron/model-manager.ts: 无 extract-zip import（D-18）
  - electron/model-manager.ts: 无 .model3.json 扫描/校验函数（D-18）
  - electron/model-manager.ts: importModelZip 为 Phase 4 placeholder
  - main process TypeScript 编译无 extract-zip 相关错误
  </done>
</task>

<task type="auto">
  <name>Task 3: package.json — Remove gsap + extract-zip dependencies</name>

  <read_first>
  - apps/desktop/package.json
  - apps/desktop/electron/model-manager.ts（确认 extract-zip 引用已移除）
  - apps/desktop/src/App.tsx（确认 gsap 引用已随 PetPerformanceDirector 移除）
  </read_first>

  <files>
  - apps/desktop/package.json
  </files>

  <action>
  从 package.json 中移除 gsap 和 extract-zip 依赖（D-19, CLEAN-07）。

  此前提条件已满足：
  - Plan 01 Task 2 已删除 pet-performance-director.ts（gsap 唯一使用者）
  - Plan 03 Task 1 已从 App.tsx 移除所有 PetPerformanceDirector 引用
  - Plan 03 Task 2 已从 model-manager.ts 移除 extract-zip import 和 .model3.json 逻辑

  操作：
  ```bash
  # 从 dependencies 中移除 gsap 和 extract-zip
  # 这将自动更新 package.json 和 package-lock.json
  npm uninstall gsap extract-zip
  ```

  如果 npm uninstall 不可用，手动编辑 package.json：
  - 从 `dependencies` 对象中删除 `"gsap": "^3.15.0",` 行
  - 从 `dependencies` 对象中删除 `"extract-zip": "^2.0.1",` 行

  验证：
  ```bash
  grep -c "gsap" apps/desktop/package.json          # 应为 0
  grep -c "extract-zip" apps/desktop/package.json     # 应为 0
  ```
  </action>

  <acceptance_criteria>
  ```bash
  grep "gsap" apps/desktop/package.json | grep -q "^0$" || echo "FAIL: gsap still in package.json"
  grep "extract-zip" apps/desktop/package.json | grep -q "^0$" || echo "FAIL: extract-zip still in package.json"
  ```
  </acceptance_criteria>

  <verify>
  <automated>
  ERR=0
  if grep -q '"gsap"' apps/desktop/package.json; then
    echo "FAIL: gsap still in package.json"; ERR=1
  else
    echo "PASS: gsap removed from package.json"
  fi
  if grep -q '"extract-zip"' apps/desktop/package.json; then
    echo "FAIL: extract-zip still in package.json"; ERR=1
  else
    echo "PASS: extract-zip removed from package.json"
  fi

  # 全量编译验证
  echo "--- Full build verification ---"
  npm run build 2>&1
  BUILD_EXIT=$?
  if [ $BUILD_EXIT -ne 0 ]; then echo "FAIL: npm run build failed"; ERR=1; fi

  exit $ERR
  </automated>
  </verify>

  <done>
  - package.json dependencies: gsap 已移除（CLEAN-07）
  - package.json dependencies: extract-zip 已移除（CLEAN-07）
  - npm run build 编译零错误
  - 项目中无代码引用 gsap 或 extract-zip
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Source code cleanup and dependency removal — no new code, no data flow, no trust boundaries introduced or modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | N/A | N/A | accept | Source file modification and dependency removal — no security impact. Removing unused dependencies (extract-zip) reduces supply chain attack surface. |
</threat_model>

<verification>
```bash
echo "=== Verification: Plan 03-03 ==="

# 1. App.tsx clean
echo "--- App.tsx: PetPerformanceDirector removal ---"
grep -rn "pet-performance-director\|performanceDirectorRef" apps/desktop/src/App.tsx \
  && { echo "❌ FAIL: PetPerformanceDirector still in App.tsx"; exit 1; } \
  || echo "✅ PASS: App.tsx cleaned"

# 2. CSS class rename
echo "--- App.tsx: CSS class rename ---"
grep -rn "live2d-container" apps/desktop/src/App.tsx \
  && { echo "❌ FAIL: .live2d-container still in App.tsx"; exit 1; } \
  || echo "✅ PASS: CSS class renamed to .rive-container"

# 3. models.json clean
echo "--- models.json: Live2D entries removed ---"
grep -c "model3.json" apps/desktop/public/assets/models/models.json \
  && { echo "❌ FAIL: Live2D entries still in models.json"; exit 1; } \
  || echo "✅ PASS: models.json cleaned"

# 4. model-manager.ts clean
echo "--- model-manager.ts: extract-zip removal ---"
grep -rn "extract-zip" apps/desktop/electron/model-manager.ts \
  && { echo "❌ FAIL: extract-zip still in model-manager.ts"; exit 1; } \
  || echo "✅ PASS: model-manager.ts cleaned"

# 5. package.json clean
echo "--- package.json: gsap + extract-zip removal ---"
if grep -q '"gsap"' apps/desktop/package.json; then
  echo "❌ FAIL: gsap still in package.json"
  exit 1
fi
if grep -q '"extract-zip"' apps/desktop/package.json; then
  echo "❌ FAIL: extract-zip still in package.json"
  exit 1
fi
echo "✅ PASS: package.json cleaned"

# 6. Full build check (D-20)
echo "--- Full TypeScript build: npm run build ---"
npm run build 2>&1
BUILD_EXIT=$?
if [ $BUILD_EXIT -eq 0 ]; then echo "✅ PASS: npm run build succeeds"; else echo "❌ FAIL: build failed"; exit 1; fi

# 7. Grep audit: no gsap or extract-zip references in source (D-21)
echo "--- Grep audit: gsap + extract-zip ---"
GSAP_MATCHES=$(grep -rn "from 'gsap'\|require('gsap')\|import.*gsap" \
  --include="*.{ts,tsx}" apps/desktop/src apps/desktop/electron 2>/dev/null | wc -l | tr -d ' ')
EXTRACT_MATCHES=$(grep -rn "extract-zip\|extractZip" \
  --include="*.{ts,tsx}" apps/desktop/src apps/desktop/electron 2>/dev/null | wc -l | tr -d ' ')
echo "gsap source refs: $GSAP_MATCHES (expected 0)"
echo "extract-zip source refs: $EXTRACT_MATCHES (expected 0)"
```
</verification>

<success_criteria>
- [ ] App.tsx: 无 PetPerformanceDirector 引用 + CSS 类名已重命名（D-15）
- [ ] public/assets/models/models.json: 无 Live2D 模型条目（D-17）
- [ ] electron/model-manager.ts: 无 extract-zip import + 无 .model3.json 逻辑（D-18）
- [ ] package.json dependencies: 无 gsap（D-19, CLEAN-07）
- [ ] package.json dependencies: 无 extract-zip（D-19, CLEAN-07）
- [ ] npm run build 编译通过（D-20）
- [ ] grep 审计确认无 gsap/extract-zip 源文件引用（D-21）
</success_criteria>

<output>
After completion, create `.planning/phases/03-live2d-cleanup/03-03-SUMMARY.md`
</output>
