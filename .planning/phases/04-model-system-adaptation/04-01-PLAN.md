---
phase: 04-model-system-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/electron/model-manager.ts
autonomous: true
requirements:
  - MODEL-02
  - MODEL-03
must_haves:
  truths:
    - "用户通过 Import Model 文件对话框可以选择 .riv 文件（而非 .zip）"
    - "用户选择的 .riv 文件被复制到 userData/models/<id>/ 目录并生成 .vivipet-registry.json"
    - "SQLite 动作索引不会对 .riv 模型路径运行"
    - "listUserModels 返回基于 .vivipet-registry.json 扫描的真实用户模型列表"
  artifacts:
    - path: "apps/desktop/electron/model-manager.ts"
      provides: "Rive .riv 模型导入、用户模型列表扫描、索引跳过守卫"
      min_lines: 240
      exports: ["importModelViaDialog", "importRiveModel", "listUserModels", "indexBundledModels"]
  key_links:
    - from: "ipc.ts (pet:model:import handler)"
      to: "importModelViaDialog()"
      via: "IPC invoke → importModelViaDialog → (importRiveModel internally)"
      pattern: "importModelViaDialog"
    - from: "ipc.ts (pet:model:listUserModels handler)"
      to: "listUserModels()"
      via: "IPC invoke → reads .vivipet-registry.json files"
      pattern: "listUserModels"
    - from: "model-registry.ts (loadModelConfigs → indexBundledModels)"
      to: "indexBundledModels()"
      via: "IPC invoke → skips .riv paths before SQLite"
      pattern: "indexBundledModels"
---

<objective>
重写 model-manager.ts 中的模型导入流程和索引逻辑，使其从 Live2D .zip 处理适配为 Rive .riv 文件处理。

Purpose: Phase 3 遗留了骨架代码（importModelZip 是 stub，listUserModels 返回空数组，indexBundledModels 无 .riv 跳过守卫）。本计划将这些函数替换为真实的 .riv 实现，同时保持 vivipet-assets 协议和 preload 接口不变。

Output:
- `apps/desktop/electron/model-manager.ts` — 修改后的文件，包含完整的 .riv 导入/扫描/跳过逻辑
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-model-system-adaptation/04-CONTEXT.md (D-04~D-08 locked decisions)
@.planning/phases/04-model-system-adaptation/04-RESEARCH.md (Pattern 1~3, Code Examples)

<!-- Current target file state — read before modifying -->
@apps/desktop/electron/model-manager.ts

<!-- IPC handlers — function signatures must remain compatible -->
@apps/desktop/electron/ipc.ts (lines 108-118)

<!-- preload — no changes needed, verify signatures match -->
@apps/desktop/electron/preload.ts (lines 17-35, 97-105)

<interfaces>
From ipc.ts (handlers that consume model-manager exports):

```typescript
// Line 5 import — WILL change: remove importModelZip, add importRiveModel
import { importModelViaDialog, indexBundledModels, listUserModels } from './model-manager';

// Line 108-118 IPC handlers — function signature constraints:
ipcMain.handle('pet:model:import', async () => {
  return importModelViaDialog();  // Must return Promise<{id,name,path,window?,actions?} | null>
});

ipcMain.handle('pet:model:listUserModels', async () => {
  return listUserModels();  // Must return Array<{id,name,path,window?,actions?}>
});

ipcMain.handle('pet:model:indexBundledModels', async (_event, models) => {
  indexBundledModels(models);  // Args: BundledModelConfig[]
});
```

From preload.ts (petModel API surface — do NOT change):

```typescript
interface PetModelAPI {
  import: () => Promise<{id:string;name:string;path:string;window?:{w:number;h:number};actions?:Record<string,unknown>} | null>;
  listUserModels: () => Promise<Array<{id:string;name:string;path:string;window?:{w:number;h:number};actions?:Record<string,unknown>}>>;
  indexBundledModels: (models: Array<{id:string;name:string;path:string}>) => Promise<void>;
  setCurrent: (modelId: string) => Promise<void>;
  listActions: (modelId?: string) => Promise<unknown[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace importModelViaDialog(.zip) + importModelZip → importRiveModel(.riv)</name>

  <read_first>
  @apps/desktop/electron/model-manager.ts (lines 113-155 — current importModelViaDialog + importModelZip)
  @apps/desktop/electron/ipc.ts (line 5 — current import statement)
  </read_first>

  <files>
  apps/desktop/electron/model-manager.ts
  </files>

  <action>
  **做什么：** 将 `importModelViaDialog()` 的文件过滤器从 .zip 改为 .riv，删除 `importModelZip()` 函数，新增 `importRiveModel(rivFilePath: string)` 函数（处理单文件复制 + 注册表写入）。

  **具体实现：**

  1. **修改 `importModelViaDialog()` (lines 122-140):**
     - 将 `title` 从 `'Select ViviPet Model Package'` 改为 `'Select Rive Model (.riv)'`
     - 将 `filters` 从 `[{ name: 'ViviPet Model Package (.zip)', extensions: ['zip'] }]` 改为 `[{ name: 'Rive Model (.riv)', extensions: ['riv'] }]`（per D-04）
     - 将 `return importModelZip(result.filePaths[0])` 改为 `return importRiveModel(result.filePaths[0])`（per D-06）
     - 保持现有的 `result.canceled || result.filePaths.length === 0 → return null` 不变

  2. **删除旧函数 `importModelZip()` (lines 146-155):**
     - 完全移除 `importModelZip` 函数及其 JSDoc 注释

  3. **新增 `importRiveModel()` 函数（在 importModelViaDialog 之后）:**
     - 函数签名：`async function importRiveModel(rivFilePath: string): Promise<{ id: string; name: string; path: string; window?: { width: number; height: number }; actions?: Record<string, RendererActionConfig> } | null>`
     - 实现逻辑（per D-05, D-06）:
       ```typescript
       const fileName = path.basename(rivFilePath, '.riv');
       const modelId = toModelId(fileName);
       const displayName = fileName;
       const modelDir = path.join(app.getPath('userData'), USER_MODELS_DIR, modelId);
       
       // 1. 创建目标目录
       fs.mkdirSync(modelDir, { recursive: true });
       
       // 2. 复制 .riv 文件
       const destPath = path.join(modelDir, 'model.riv');
       fs.copyFileSync(rivFilePath, destPath);
       
       // 3. 写 .vivipet-registry.json
       const registry = {
         id: modelId,
         name: displayName,
         path: `vivipet-assets://models/${modelId}/model.riv`,
         type: 'rive',
         window: { width: 520, height: 760 },
       };
       fs.writeFileSync(
         path.join(modelDir, '.vivipet-registry.json'),
         JSON.stringify(registry, null, 2),
       );
       
       log.info(`[ModelManager] Imported Rive model: ${modelId} from ${rivFilePath}`);
       
       return {
         id: registry.id,
         name: registry.name,
         path: registry.path,
         window: registry.window,
       };
       ```

  **注意事项（避免什么以及原因）：**
  - **DO NOT** 在 `importRiveModel` 中返回 `actions` — Rive SM 自动管理动画，无需动作索引（per D-09）。返回 `actions: undefined` 即可。
  - **DO NOT** 使用 `extract-zip` 或任何解压逻辑 — .riv 是单文件格式。
  - **DO NOT** 删除 `copyDirectoryIfExists` 或 `findFiles` 辅助函数 — 它们可能被未来逻辑使用（虽然当前未被调用）。
  - **DO** 使用 `toModelId()` 函数来生成模型 ID（基于文件名 sanitize），保持命名策略一致。
  - 确保 `importRiveModel` 被包裹在 try/catch 中，文件操作失败时返回 null 并 log error。

  **where to place in file:** `importRiveModel` 放在原来 `importModelZip` 的位置（约 line 146），在 `importModelViaDialog` 之后。修改后函数顺序为：export functions → helper functions → export async importModelViaDialog → async importRiveModel → export listUserModels → export indexBundledModels → toRendererActions。
  </action>

  <verify>
  <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  <automated>rg "importModelZip" apps/desktop/electron/model-manager.ts || echo "REMOVED_OK: importModelZip confirmed removed"</automated>
  <automated>rg "importRiveModel" apps/desktop/electron/model-manager.ts | grep -c "importRiveModel" | xargs -I{} sh -c '[ {} -ge 1 ] && echo "PRESENT_OK: importRiveModel found" || echo "MISSING: importRiveModel not found"'</automated>
  <automated>rg "extensions.*riv" apps/desktop/electron/model-manager.ts | grep -c "riv" | xargs -I{} sh -c '[ {} -ge 1 ] && echo "PRESENT_OK: .riv filter found" || echo "MISSING: .riv filter not found"'</automated>
  </verify>

  <done>
  - TypeScript 编译无错误
  - model-manager.ts 中没有 `importModelZip` 引用
  - model-manager.ts 中有 `importRiveModel` 函数声明
  - importModelViaDialog 的 dialog filter 为 `extensions: ['riv']`
  </done>
</task>

<task type="auto">
  <name>Task 2: Reimplement listUserModels to scan .vivipet-registry.json</name>

  <read_first>
  @apps/desktop/electron/model-manager.ts (lines 161-170 — current stub listUserModels)
  @apps/desktop/electron/preload.ts (lines 25-31 — PetModelAPI return type)
  </read_first>

  <files>
  apps/desktop/electron/model-manager.ts
  </files>

  <action>
  **做什么：** 将 `listUserModels()` 从返回空数组的 stub 重写为扫描 `userData/models/*/.vivipet-registry.json` 的真实实现（per D-07）。

  **具体实现（替换 lines 161-170 的当前 stub）：**

  ```typescript
  /**
   * Scan userData/models/ for imported Rive models and return their configs.
   * Each model directory should contain a .vivipet-registry.json file
   * created during importModelViaDialog/importRiveModel.
   */
  export function listUserModels(): Array<{
    id: string;
    name: string;
    path: string;
    window?: { width: number; height: number };
    actions?: Record<string, RendererActionConfig>;
  }> {
    const modelsDir = path.join(app.getPath('userData'), USER_MODELS_DIR);
    if (!fs.existsSync(modelsDir)) {
      return [];
    }

    const results: Array<{
      id: string;
      name: string;
      path: string;
      window?: { width: number; height: number };
      actions?: Record<string, RendererActionConfig>;
    }> = [];

    const entries = fs.readdirSync(modelsDir, { withFileTypes: true });

    for (const entry of entries) {
      if (!entry.isDirectory()) continue;

      const registryPath = path.join(modelsDir, entry.name, '.vivipet-registry.json');
      if (!fs.existsSync(registryPath)) continue;

      try {
        const data = JSON.parse(fs.readFileSync(registryPath, 'utf-8'));
        if (data.id && data.name && data.path) {
          results.push({
            id: data.id,
            name: data.name,
            path: data.path,
            window: data.window,
          });
        }
      } catch (err) {
        log.warn(`[ModelManager] Failed to parse registry: ${registryPath}`, err);
        // Per Pitfall 5: one bad registry should not block all user models
      }
    }

    return results;
  }
  ```

  **注意事项（避免什么以及原因）：**
  - **DO NOT** 修改返回类型 — 必须与 preload.ts 中的 `PetModelAPI.listUserModels` 签名兼容（per D-07 要求 preload.ts 零改动）。
  - **DO** 对每个 registry 读取使用 try/catch 包裹，隔离单个 registry JSON 解析错误（per RESEARCH Pitfall 5）。
  - **DO** 如果 `userData/models/` 目录不存在（首次启动未导入模型），直接返回空数组，不要创建目录。
  - **DO NOT** 创建目录 — `listUserModels` 是只读扫描，不应该有副作用。
  </action>

  <verify>
  <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  <automated>rg "vivipet-registry" apps/desktop/electron/model-manager.ts | grep -c "vivipet-registry" | xargs -I{} sh -c '[ {} -ge 1 ] && echo "PRESENT_OK: .vivipet-registry.json scanning found" || echo "MISSING: .vivipet-registry.json scanning not found"'</automated>
  <automated>rg "log\.warn.*registry" apps/desktop/electron/model-manager.ts | grep -c "warn" | xargs -I{} sh -c '[ {} -ge 1 ] && echo "PRESENT_OK: try/catch error handling found" || echo "MISSING: try/catch not found"'</automated>
  </verify>

  <done>
  - TypeScript 编译无错误
  - listUserModels 扫描 `userData/models/*/.vivipet-registry.json` 而非返回空数组
  - 单个 registry 解析失败时使用 try/catch 隔离（不影响其他模型）
  - 返回类型与 preload.ts PetModelAPI 签名兼容
  </done>
</task>

<task type="auto">
  <name>Task 3: Add .riv skip guard in indexBundledModels</name>

  <read_first>
  @apps/desktop/electron/model-manager.ts (lines 172-184 — current indexBundledModels)
  @apps/desktop/electron/action-index.ts (lines 128-161 — scanModelActions, for context on why skip is needed)
  </read_first>

  <files>
  apps/desktop/electron/model-manager.ts
  </files>

  <action>
  **做什么：** 在 `indexBundledModels()` 中添加 .riv 路径跳过守卫，确保 Rive 模型不触发 SQLite 动作索引（per D-08）。

  **具体实现：**

  将当前 `indexBundledModels` 函数体（lines 173-184）：

  ```typescript
  export function indexBundledModels(models: BundledModelConfig[]): void {
    for (const model of models) {
      const modelPath = resolveBundledModelPath(model.path);
      if (!modelPath) continue;

      indexModelActions({
        modelId: model.id,
        name: model.name,
        modelPath,
        rootDir: path.dirname(modelPath),
      });
    }
  }
  ```

  修改为：

  ```typescript
  export function indexBundledModels(models: BundledModelConfig[]): void {
    for (const model of models) {
      // D-08: Skip .riv models — Rive State Machine handles animation internally
      if (model.path.endsWith('.riv')) {
        log.info(`[ModelManager] Skipping SQLite index for Rive model: ${model.id}`);
        continue;
      }

      const modelPath = resolveBundledModelPath(model.path);
      if (!modelPath) continue;

      indexModelActions({
        modelId: model.id,
        name: model.name,
        modelPath,
        rootDir: path.dirname(modelPath),
      });
    }
  }
  ```

  **注意事项（避免什么以及原因）：**
  - **DO** 在 `resolveBundledModelPath()` 之前检查 `model.path.endsWith('.riv')` — 这是检查 model config 对象的 path 字段，而非文件系统路径。这样可以避免读取不存在的 .riv 文件（per RESEARCH Pitfall 2）。
  - **DO NOT** 使用 `path.extname()` 代替 `model.path.endsWith('.riv')` — 两种方式效果相同，但 `endsWith` 更直接且与 RESEARCH 代码示例一致。
  - **DO** 添加清晰的注释引用 D-08 决策 ID，便于追溯。
  - 跳过时使用 `log.info` 而非 `console.warn` — 这是正常行为，不是警告。
  - 验证 `import` 语句是否已导入 `log`（已有 `import log from 'electron-log'` 在 line 5）。
  </action>

  <verify>
  <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  <automated>rg "endsWith\('\.riv'\)" apps/desktop/electron/model-manager.ts | grep -c "riv" | xargs -I{} sh -c '[ {} -ge 1 ] && echo "PRESENT_OK: .riv skip guard found" || echo "MISSING: .riv skip guard not found"'</automated>
  <automated>rg "Skipping SQLite index" apps/desktop/electron/model-manager.ts | grep -c "Skipping" | xargs -I{} sh -c '[ {} -ge 1 ] && echo "PRESENT_OK: skip log message found" || echo "MISSING: skip log message not found"'</automated>
  <automated>rg -n "indexModelActions" apps/desktop/electron/model-manager.ts | grep -v "import " | wc -l | xargs -I{} sh -c '[ {} -ge 3 ] && echo "CHECK: indexModelActions calls remain for non-riv (expected ≥3: import + skip + if-block)" || echo "WARN: fewer indexModelActions references than expected"'</automated>
  </verify>

  <done>
  - TypeScript 编译无错误
  - indexBundledModels 在遇到 `model.path` 以 `.riv` 结尾时跳过 SQLite 索引
  - 跳过时有 info 级别日志输出
  - 非 .riv 路径的索引逻辑保持不变
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main Process → Renderer | IPC invoke 返回用户模型配置 |
| File Dialog → Main Process | 用户通过 OS dialog 选择 .riv 文件路径 |
| .vivipet-registry.json → Main Process | 本地 JSON 文件被解析为模型配置 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-001 | Tampering | importRiveModel 中的文件复制 | mitigate | 复制目标限制在 `userData/models/<id>/` 内（由 `toModelId` sanitize + `path.join` 约束） |
| T-04-002 | DoS | listUserModels JSON 解析 | mitigate | 每个 registry 读取用 try/catch 包裹，单个解析错误不阻塞全部模型（per RESEARCH Pitfall 5） |
| T-04-003 | Tampering | indexBundledModels 跳过守卫 | mitigate | 守卫检查 `model.path.endsWith('.riv')` — 作用于 model config 对象的 path 字段，而非用户可控的输入（per RESEARCH Pitfall 2） |
| T-04-004 | Information Disclosure | vivipet-assets 路径遍历 | accept | 已有 path traversal 防护 `resolveSafeUserModelPath()`（model-manager.ts l.30-38）。本计划不修改协议处理逻辑。 |
</threat_model>

<verification>
1. **TypeScript 编译检查：** `cd apps/desktop && npx tsc --noEmit` 无错误
2. **函数存在性检查：** 验证 importRiveModel、listUserModels（非 stub）、indexBundledModels（含 skip）均存在
3. **文件路径检查：** `rg "importModelZip" apps/desktop/electron/` 无匹配（确认完全移除）
4. **Dialog filter 检查：** `rg "extensions.*riv" apps/desktop/electron/model-manager.ts` 匹配成功
</verification>

<success_criteria>
1. TypeScript 编译通过（无类型错误）
2. model-manager.ts 没有 importModelZip 的任何引用
3. model-manager.ts 导出了 importModelViaDialog、importRiveModel（新增）、listUserModels（重写）、indexBundledModels（增强）
4. importRiveModel 使用 fs.copyFileSync + .vivipet-registry.json 写入
5. listUserModels 扫描 `.vivipet-registry.json` 而非返回空数组
6. indexBundledModels 在 `model.path.endsWith('.riv')` 时跳过 sqlite 索引
7. 所有导出函数签名与 ipc.ts handlers 兼容
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-system-adaptation/04-01-SUMMARY.md`
</output>
