---
phase: 04-model-system-adaptation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/desktop/electron-builder.yml
  - apps/desktop/RIVE_MODEL_INTEGRATION.md
autonomous: true
requirements:
  - MODEL-04
  - MODEL-05
must_haves:
  truths:
    - "electron-builder.yml 不再引用已删除的 public/models 目录"
    - "存在 RIVE_MODEL_INTEGRATION.md 文档，说明如何接入 .riv 模型"
    - "RIVE_MODEL_INTEGRATION.md 包含 SM 输入常量表、models.json 示例模板、vivipet-assets 协议说明"
    - "全套 Phase 4 验证标准可通过（D-21~D-24）"
  artifacts:
    - path: "apps/desktop/electron-builder.yml"
      provides: "Electron 打包配置（无 dangling extraResources 引用）"
      does_not_contain: "public/models"
    - path: "apps/desktop/RIVE_MODEL_INTEGRATION.md"
      provides: ".riv 模型集成用户文档"
      min_lines: 60
  key_links:
    - from: "electron-builder.yml extraResources"
      to: "打包后的 assets/ 目录"
      via: "extraResources → from: assets, to: assets"
      pattern: "from: assets"
    - from: "RIVE_MODEL_INTEGRATION.md"
      to: "rive-inputs.ts"
      via: "文档引用 SM 输入常量（state/mouth_open/look_x/look_y/blink/breathe）"
    - from: "RIVE_MODEL_INTEGRATION.md"
      to: "model-registry.ts"
      via: "文档提供 models.json 模板（type: 'rive'）"
---

<objective>
修复 electron-builder.yml 的 dangling extraResources 引用，创建 RIVE_MODEL_INTEGRATION.md 用户文档，并对 Phase 4 全部改动进行最终验证。

Purpose:
- electron-builder.yml 仍引用已删除的 `public/models` 目录（Phase 3 已清理）。继续存在会导致 electron-builder 在打包时警告或报错（per D-16）。
- Phase 4 需要一份文档说明 Rive 模型接入方式，降低用户的使用门槛（per MODEL-05, D-18~D-20）。
- 本计划依赖 Plan 04-01 先完成 model-manager.ts 的重写，因为文档中描述的行为需要与代码实现一致。

Output:
- `apps/desktop/electron-builder.yml` — 移除 `public/models` extraResources 引用
- `apps/desktop/RIVE_MODEL_INTEGRATION.md` — 新用户文档
</objective>

<execution_context>
@/Users/suntc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/suntc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-model-system-adaptation/04-CONTEXT.md (D-16~D-24 locked decisions)
@.planning/phases/04-model-system-adaptation/04-RESEARCH.md (Pitfall 4, Assumption A3, Code Examples)

<!-- Plan 04-01 just completed — model-manager.ts has importRiveModel + listUserModels + .riv skip guard -->
@.planning/phases/04-model-system-adaptation/04-01-SUMMARY.md

<!-- Target files -->
@apps/desktop/electron-builder.yml
@apps/desktop/src/features/pet/rive-inputs.ts

<interfaces>
From electron-builder.yml (current state — line 12-13 to remove):

```yaml
extraResources:
  - from: assets
    to: assets
  - from: public/models    # ← REMOVE this line (per D-16)
    to: models             # ← REMOVE this line (per D-16)
```

Target state:

```yaml
extraResources:
  - from: assets
    to: assets
```

From rive-inputs.ts (for documentation content):

```typescript
export const RIVE_INPUTS = {
  STATE: 'state',           // Number 0-9
  BLINK_TRIGGER: 'blink',   // Trigger
  BREATHE_TRIGGER: 'breathe', // Trigger
  MOUTH_OPEN: 'mouth_open', // Number 0.0-1.0
  LOOK_X: 'look_x',         // Number -1.0-1.0
  LOOK_Y: 'look_y',         // Number -1.0-1.0
} as const;

export type RiveStateValue = 'idle' | 'thinking' | 'speaking' | 'happy' | 'error'
  | 'searching' | 'coding' | 'terminal' | 'confused' | 'angry';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dangling public/models reference from electron-builder.yml</name>

  <read_first>
  @apps/desktop/electron-builder.yml (entire file — 35 lines)
  </read_first>

  <files>
  apps/desktop/electron-builder.yml
  </files>

  <action>
  从 `electron-builder.yml` 的 `extraResources` 列表中移除已删除的 `public/models` 目录引用（per D-16, D-17）。

  **具体操作：**
  1. 删除以下两行：
     ```yaml
       - from: public/models
         to: models
     ```
  2. 保留 `assets/` 的 extraResources 条目不变。
  3. 确保 YAML 格式正确（缩进一致）。

  **修改后 extraResources 段落：**
  ```yaml
  extraResources:
    - from: assets
      to: assets
  ```

  **注意事项（避免什么以及原因）：**
  - **DO NOT** 删除 `assets/` 的 extraResources 条目 — assets/ 目录仍在（包含 icon.png、models.json 等）。
  - **DO NOT** 修改 `from: assets, to: assets` 的缩进或内容。
  - **DO NOT** 创建 `public/models/` 目录来替代删除的引用（per D-17 明确延后到未来）。
  - 删除后验证 YAML 格式：确保 `extraResources:` 下的条目缩进一致（使用 2 空格缩进）。
  - 运行 `npm run build` 确保打包不会因 missing dir 而失败。
  </action>

  <verify>
  <automated>rg "public/models" apps/desktop/electron-builder.yml; test $? -eq 1 && echo "REMOVED_OK: public/models fully removed" || echo "FAIL: public/models still present"</automated>
  <automated>node -e "const fs=require('fs'); const y=require('js-yaml')||{load:(s)=>JSON.parse(JSON.stringify(require('yaml').parse(s)))}; const d=y.load(fs.readFileSync('apps/desktop/electron-builder.yml','utf8')); const froms=d.extraResources.map(e=>e.from); console.log(froms.includes('assets')?'ASSETS_OK':'ASSETS_MISSING')" 2>/dev/null || echo "YAML_CHECK_SKIPPED: no yaml parser available"</automated>
  </verify>

  <done>
  - electron-builder.yml 中 `public/models` 被完全移除
  - `assets` 的 extraResources 条目保留且格式正确
  - YAML 缩进一致（2 空格）
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RIVE_MODEL_INTEGRATION.md user documentation</name>

  <read_first>
  @apps/desktop/src/features/pet/rive-inputs.ts (SM input constants)
  @apps/desktop/src/features/pet/model-registry.ts (ModelConfig type)
  @.planning/phases/04-model-system-adaptation/04-CONTEXT.md (lines 46-48 — D-18~D-20 content requirements)
  @.planning/phases/04-model-system-adaptation/04-RESEARCH.md (lines 279-309 — model examples)
  </read_first>

  <files>
  apps/desktop/RIVE_MODEL_INTEGRATION.md
  </files>

  <action>
  在 `apps/desktop/` 下创建 `RIVE_MODEL_INTEGRATION.md` 文档（per D-18）。

  **文档内容要求（per D-19, D-20）：**

  1. **SM 输入命名约定表** — 列出 `rive-inputs.ts` 中的全部常量：
     ```
     | Input Name    | Type    | Range      | Purpose                        |
     |--------------|---------|------------|--------------------------------|
     | state        | Number  | 0-9        | 主动画状态 (0:idle..9:angry)   |
     | mouth_open   | Number  | 0.0-1.0    | TTS 嘴型同步振幅               |
     | look_x       | Number  | -1.0-1.0   | 鼠标水平位置                    |
     | look_y       | Number  | -1.0-1.0   | 鼠标垂直位置                    |
     | blink        | Trigger | fire       | 眨眼动画触发                    |
     | breathe      | Trigger | fire       | 呼吸动画触发                    |
     ```

  2. **`state` 值映射表** — 从 `RIVE_STATES` 常量中提取：
     ```
     | Value      | Label     | Description      |
     |------------|-----------|------------------|
     | 0          | Idle      | 空闲状态         |
     | 1          | Thinking  | 思考中           |
     | 2          | Speaking  | 说话中           |
     | ...        | ...       | ...              |
     ```

  3. **.riv 文件放置位置说明：**
     - **内置模型：** 放入 `apps/desktop/public/models/<Name>/` 目录（需自行创建），文件名 `<Name>.riv`
     - **用户导入模型：** 通过应用内 "Import Model" 功能（tray 菜单），选择 .riv 文件自动导入

  4. **models.json 配置示例（per D-20）：**
     ```json
     {
       "models": [
         {
           "id": "my-pet",
           "name": "My Pet",
           "path": "/models/my-pet/my-pet.riv",
           "type": "rive",
           "window": {
             "width": 520,
             "height": 760
           }
         }
       ]
     }
     ```

  5. **vivipet-assets 协议说明（per D-19）：**
     - 用户导入的模型通过 `vivipet-assets://models/<modelId>/model.riv` 访问
     - 协议在应用启动时自动注册，有路径穿越防护
     - 渲染器通过 `fetch(url) → ArrayBuffer → new Rive({ buffer })` 使用

  6. **项目相关文件和路径汇总表**

  **文件位置：** `apps/desktop/RIVE_MODEL_INTEGRATION.md`（per  discretion — 放在 desktop 应用目录，与 electron-builder.yml 同目录，表示这是桌面应用特定的集成指南）

  **注意事项（避免什么以及原因）：**
  - **DO NOT** 将文件放在项目根目录或 docs/ 目录 — discretion 允许选择，建议放在 `apps/desktop/` 以保持与桌面应用的其他文档一致。
  - **DO NOT** 包含 Live2D 相关内容 — 本文档只针对 Rive .riv。
  - **DO NOT** 包含 Action Index / SQLite 相关内容 — Rive SM 自动管理动画，无需索引（per D-09）。
  - **DO** 使用中文编写主要说明（与项目其他文档一致），示例代码和常量表用英文。
  - **DO** 使用 fenced code blocks 展示所有代码/配置示例（```json / ```typescript）。
  - **DO** 在各章节中引用对应的决策 ID（如 D-03, D-10 等）以便追踪。

  <is_file_new>true</is_file_new>
  </action>

  <verify>
  <automated>test -f apps/desktop/RIVE_MODEL_INTEGRATION.md && echo "EXISTS_OK: RIVE_MODEL_INTEGRATION.md created" || echo "MISSING: file not found"</automated>
  <automated>wc -l < apps/desktop/RIVE_MODEL_INTEGRATION.md | xargs -I{} sh -c '[ {} -ge 60 ] && echo "LINES_OK: {} lines" || echo "TOO_SHORT: {} lines (<60)"'</automated>
  <automated>grep -c "vivipet-assets" apps/desktop/RIVE_MODEL_INTEGRATION.md | xargs -I{} sh -c '[ {} -ge 1 ] && echo "PROTOCOL_OK" || echo "PROTOCOL_MISSING"'</automated>
  <automated>grep -c "models.json" apps/desktop/RIVE_MODEL_INTEGRATION.md | xargs -I{} sh -c '[ {} -ge 1 ] && echo "TEMPLATE_OK" || echo "TEMPLATE_MISSING"'</automated>
  <automated>grep -c "mouth_open" apps/desktop/RIVE_MODEL_INTEGRATION.md | xargs -I{} sh -c '[ {} -ge 1 ] && echo "INPUTS_OK" || echo "INPUTS_MISSING"'</automated>
  </verify>

  <done>
  - apps/desktop/RIVE_MODEL_INTEGRATION.md 文件创建完成
  - 文档包含：
    * SM 输入常量表（state/mouth_open/look_x/look_y/blink/breathe）
    * state 值映射表（0=idle .. 9=angry）
    * models.json 配置示例模板
    * vivipet-assets 协议说明
    * .riv 文件放置位置说明
  - 文档行数 >= 60
  </done>
</task>

<task type="auto">
  <name>Task 3: Full Phase 4 verification audit</name>

  <read_first>
  @.planning/phases/04-model-system-adaptation/04-RESEARCH.md (lines 443-463, Validation Architecture)
  </read_first>

  <files>
  apps/desktop/electron-builder.yml
  apps/desktop/RIVE_MODEL_INTEGRATION.md
  apps/desktop/electron/model-manager.ts
  </files>

  <action>
  对所有 Phase 4 改动进行端到端验证，确认所有 D-21~D-24 验证标准可满足。

  **验证清单：**

  1. **TypeScript 编译检查：**
     运行 `cd apps/desktop && npx tsc --noEmit`
     确保所有 TypeScript 文件编译无错误。

  2. **完整构建测试：**
     运行 `cd apps/desktop && npm run build`
     确保 main process + renderer 都构建通过。

  3. **D-21 验证——vivipet-assets 协议初始化：**
     grep 确认 `initModelProtocol` 在 `main.ts` 中仍被调用，注册日志存在。

  4. **D-22 验证——无模型时 fallback：**
     grep 确认 `PetStage.tsx` 中 `!modelLoaded` 或 `loadError` 的 fallback 逻辑存在（非本计划修改范围，仅是确认不改）。

  5. **D-23 验证——SQLite 索引不对 .riv 运行：**
     grep 确认 `indexBundledModels` 中有 `.riv` 跳过守卫。

  6. **D-24 验证——electron-builder 不因 missing dir 失败：**
     grep 确认 `public/models` 从 electron-builder.yml 中完全移除。

  7. **MODEL-01 验证——类型系统：**
     grep 确认 `model-registry.ts` 中 `ModelType = 'rive'`（已在 Phase 1-3 完成，仅确认未回退）。

  8. **MODEL-05 验证——文档存在性：**
     确认 `apps/desktop/RIVE_MODEL_INTEGRATION.md` 存在。

  **注意事项：**
  - **DO NOT** 启动 Electron 应用 — GUI 验证留给 `/gsd-verify-work` 阶段。
  - **DO** 如果没有 yaml 解析器可用，使用 grep 验证 electron-builder.yml 格式。
  - 记录所有验证结果到 SUMMARY 中。
  </action>

  <verify>
  <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | tail -5; echo "---"; rg "endsWith\('.riv'\)" apps/desktop/electron/model-manager.ts && echo "GUARD_OK: .riv skip guard present in indexBundledModels" || echo "GUARD_MISSING"</automated>
  <automated>rg "public/models" apps/desktop/electron-builder.yml; test $? -eq 1 && echo "YAML_OK: public/models removed" || echo "YAML_FAIL: public/models still present"</automated>
  <automated>rg "initModelProtocol" apps/desktop/electron/main.ts && echo "PROTOCOL_OK: initModelProtocol still registered" || echo "PROTOCOL_MISSING"</automated>
  <automated>test -f apps/desktop/RIVE_MODEL_INTEGRATION.md && echo "DOCS_OK: documentation exists" || echo "DOCS_MISSING"</automated>
  </verify>

  <done>
  - TypeScript 编译完全通过
  - ESM/Vite build 通过
  - electron-builder.yml 无 public/models 引用（D-24）
  - indexBundledModels 包含 .riv 跳过守卫（D-23）
  - vivipet-assets 协议初始化确认（D-21）
  - RIVE_MODEL_INTEGRATION.md 文档存在且完整（MODEL-05）
  - 无模型 fallback 逻辑未被意外移除（D-22）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| electron-builder.yml → 打包流程 | 构建配置决定哪些资源被打包到应用中 |
| Documentation → User | 用户文档指引的配置操作可能被用户直接使用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-005 | DoS | electron-builder.yml 格式错误 | mitigate | 删除 extraResources 条目后运行 npm run build 确认 YAML 无误 |
| T-04-006 | Tampering | 文档中的路径指引错误 | accept | 文档仅为人类开发者参考，不自动执行；路径在 Task 3 greps 中已被覆盖验证 |
</threat_model>

<verification>
1. **electron-builder.yml：** grep 确认 `public/models` 被完全移除，`assets` 条目保留
2. **RIVE_MODEL_INTEGRATION.md：** 文件存在（>=60 行），包含 SM 输入表 + models.json 模板 + vivipet-assets 说明
3. **TypeScript 编译：** `cd apps/desktop && npx tsc --noEmit` 无错误
4. **完整构建：** `cd apps/desktop && npm run build` 无错误
5. **D-21~D-24 验证：** 全部 grep 确认通过
</verification>

<success_criteria>
1. electron-builder.yml 没有 `public/models` 引用（D-16, D-24）
2. RIVE_MODEL_INTEGRATION.md 存在于 `apps/desktop/`，包含 SM 输入常量表（D-10, D-19）
3. 文档包含 models.json 配置示例（D-20）
4. 文档包含 vivipet-assets 协议说明（D-19）
5. TypeScript 编译通过
6. full build 通过
7. Phase 4 所有验证标准（D-21~D-24）自动化部分通过 grep 确认
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-system-adaptation/04-02-SUMMARY.md`
</output>
